# orch v4 实现计划 — 取消/并发/成本/预算/审批/隔离

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 orch 补齐调研发现的关键能力:任务取消/停止+事件历史、并发上限、token/成本统计、预算熔断、可选审批+可编辑计划、git worktree 与 opt-in 容器隔离。

**Architecture:** SQLite 加 events/usage 表与 tasks/agents 新列;engine 加信号量与取消检查并透传 onChild/onUsage;claude 适配器改 stream-json 解析(文本+usage);runner 落事件、报成本、支持审批暂停与预算熔断;server 加取消/审批端点与内存运行态注册表;前端加停止按钮/成本指标/审批可编辑计划/新建任务选项。引擎其余不变。

**Tech Stack:** Node 18+,express,ws,better-sqlite3,js-yaml,内置 `node --test`。前端原生 JS + dc 运行时。

## Global Constraints

- 依赖只允许:express、ws、better-sqlite3、js-yaml。不新增依赖(Docker 通过 child_process 调用,非 npm 依赖)。
- 平台 Windows:spawn `shell:true`;杀进程树用 `taskkill /T /F /PID`,`child.kill()` 兜底。
- Agent 适配器接口保持 `run({prompt,workdir,onLine,onChild,onUsage})=>Promise<{output,success}>`(新增可选 onChild/onUsage,旧调用兼容)。
- 容器隔离仅对自定义 agent;claude/codex 永远本地(依赖宿主登录)。
- 保持轻量:不引 Docker/Langfuse 为硬依赖;成本走 SQLite 自记。

---

## 阶段 P0

### Task 1: store — events / usage 表 + tasks/agents 新列

**Files:**
- Modify: `orch/store.js`
- Test: `orch/test/store4.test.js`

**Interfaces:**
- Produces: `addEvent(taskId,type,data)`、`getEvents(taskId)`、`addUsage(taskId,stepId,agent,u)`、`taskUsage(taskId)=>{input,output,cost}`、`usageToday()=>{...}`;`createTask(text,project,owner,opts)` 接受 `opts={budget,approve,isolate}`;agents 含 `pricing,image`。

- [ ] **Step 1: 写失败测试**

`orch/test/store4.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { open } = require('../store');

test('events 追加与读取', () => {
  const s = open(':memory:'); s.seed();
  const id = s.createTask('x', 'p', 'o');
  s.addEvent(id, 'status', { step: 'dev', v: 'running' });
  const ev = s.getEvents(id);
  assert.equal(ev.length, 1);
  assert.equal(JSON.parse(ev[0].data).step, 'dev');
});
test('usage 累计', () => {
  const s = open(':memory:'); s.seed();
  const id = s.createTask('x', 'p', 'o');
  s.addUsage(id, 'dev', 'claude', { input: 100, output: 50, cost: 0.01 });
  s.addUsage(id, 'test', 'codex', { input: 20, output: 10, cost: 0.002 });
  const u = s.taskUsage(id);
  assert.equal(u.input, 120); assert.equal(u.output, 60);
  assert.ok(Math.abs(u.cost - 0.012) < 1e-9);
});
test('createTask 接受 opts', () => {
  const s = open(':memory:'); s.seed();
  const id = s.createTask('x', 'p', 'o', { budget: 5000, approve: 1, isolate: 'worktree' });
  const t = s.listTasks().find((t) => t.id === id);
  assert.equal(t.budget, 5000); assert.equal(t.approve, 1); assert.equal(t.isolate, 'worktree');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/store4.test.js`
预期:FAIL（addEvent 不存在 / opts 未落库）。

- [ ] **Step 3: 实现**

`store.js` 建表区追加:
```js
    CREATE TABLE IF NOT EXISTS events(
      id INTEGER PRIMARY KEY, task_id INTEGER, ts TEXT, type TEXT, data TEXT);
    CREATE TABLE IF NOT EXISTS usage(
      id INTEGER PRIMARY KEY, task_id INTEGER, step_id TEXT, agent TEXT,
      input_tokens INTEGER, output_tokens INTEGER, cost REAL, ts TEXT);
```
tasks 表列改为含 `budget REAL, approve INTEGER, isolate TEXT`(加到现有列后)。agents 表列加 `pricing TEXT, image TEXT`。

`createTask` 改签名:
```js
    createTask(text, project, owner, opts) {
      const now = new Date().toISOString();
      const o = opts || {};
      return db.prepare('INSERT INTO tasks(text,status,project,owner,budget,approve,isolate,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?)')
        .run(text, 'pending', project || '默认项目', owner || '操作者', o.budget || 0, o.approve ? 1 : 0, o.isolate || 'none', now, now).lastInsertRowid;
    },
```
`listTasks` SELECT 加 `budget,approve,isolate`。

返回对象追加方法:
```js
    addEvent(taskId, type, data) { db.prepare('INSERT INTO events(task_id,ts,type,data) VALUES(?,?,?,?)').run(taskId, new Date().toISOString(), type, JSON.stringify(data == null ? null : data)); },
    getEvents(taskId) { return db.prepare('SELECT * FROM events WHERE task_id=? ORDER BY id').all(taskId); },
    addUsage(taskId, stepId, agent, u) { db.prepare('INSERT INTO usage(task_id,step_id,agent,input_tokens,output_tokens,cost,ts) VALUES(?,?,?,?,?,?,?)').run(taskId, stepId, agent, (u && u.input) || 0, (u && u.output) || 0, (u && u.cost) || 0, new Date().toISOString()); },
    taskUsage(taskId) { const r = db.prepare('SELECT COALESCE(SUM(input_tokens),0) i, COALESCE(SUM(output_tokens),0) o, COALESCE(SUM(cost),0) c FROM usage WHERE task_id=?').get(taskId); return { input: r.i, output: r.o, cost: r.c }; },
    usageToday() { const day = new Date().toISOString().slice(0, 10); const r = db.prepare("SELECT COALESCE(SUM(input_tokens),0) i, COALESCE(SUM(output_tokens),0) o, COALESCE(SUM(cost),0) c FROM usage WHERE substr(ts,1,10)=?").get(day); return { input: r.i, output: r.o, cost: r.c }; },
```
`addAgent`/`updateAgent` 增 `pricing,image` 字段(INSERT/UPDATE 加这两列,缺省 `pricing` 存 `JSON.stringify(d.pricing||null)`,`image` 存 `d.image||''`)。seed 的 claude/codex 给 `pricing`(claude `{in:3,out:15}`、codex `{in:1.25,out:10}` 每百万 token 美元,示意值)。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/store4.test.js`
预期:PASS,3 用例。

- [ ] **Step 5: 回归 + Commit**

Run: `cd orch && node --test`（owner/列变更不破坏旧测）
```bash
git add orch/store.js orch/test/store4.test.js
git commit -m "feat: store events/usage 表 + tasks(budget/approve/isolate)/agents(pricing/image)"
```

---

### Task 2: engine — 并发信号量 + 取消检查 + onChild/onUsage 透传

**Files:**
- Modify: `orch/engine.js`
- Test: `orch/test/engine2.test.js`

**Interfaces:**
- Consumes: `ctx` 增 `isCancelled()`、`onChild(child)`、`onUsage(stepId,agent,u)`(均可选);全局并发上限 `ORCH_CONCURRENCY`(默认 3)。
- Produces: 取消后不再起新 step;同时运行 step 数 ≤ 上限;adapter 的 onChild/onUsage 透传到 ctx。

- [ ] **Step 1: 写失败测试**

`orch/test/engine2.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { runPlan } = require('../engine');

function ctx(adapters, extra) {
  return Object.assign({ adapters, workspace: { make: () => '.' }, onLog: () => {}, onStatus: () => {} }, extra || {});
}

test('并发不超过上限', async () => {
  process.env.ORCH_CONCURRENCY = '2';
  let cur = 0, peak = 0;
  const slow = { async run() { cur++; peak = Math.max(peak, cur); await new Promise((r) => setTimeout(r, 30)); cur--; return { output: '', success: true }; } };
  const steps = Array.from({ length: 6 }, (_, i) => ({ id: 's' + i, agent: 'a', prompt: 'p', deps: [] }));
  await runPlan({ steps }, ctx({ a: slow }));
  assert.ok(peak <= 2, 'peak=' + peak);
  delete process.env.ORCH_CONCURRENCY;
});

test('取消后不再起新 step', async () => {
  let ran = 0;
  const a = { async run() { ran++; return { output: '', success: true }; } };
  let cancelled = false;
  const c = ctx({ a }, { isCancelled: () => cancelled });
  // a 依赖链:s0 -> s1;在 s0 后置取消
  const a2 = { async run() { ran++; cancelled = true; return { output: '', success: true }; } };
  const done = await runPlan({ steps: [{ id: 's0', agent: 'b', prompt: 'p', deps: [] }, { id: 's1', agent: 'a', prompt: 'p', deps: ['s0'] }] }, ctx({ a, b: a2 }, { isCancelled: () => cancelled }));
  assert.equal(ran, 1); // 只有 s0(b)跑了,取消后 s1 不跑
});

test('onUsage 透传', async () => {
  const got = [];
  const a = { async run({ onUsage }) { onUsage && onUsage({ input: 5, output: 3, cost: 0.001 }); return { output: '', success: true }; } };
  await runPlan({ steps: [{ id: 's', agent: 'a', prompt: 'p', deps: [] }] }, ctx({ a }, { onUsage: (sid, ag, u) => got.push([sid, ag, u.input]) }));
  assert.deepEqual(got, [['s', 'a', 5]]);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/engine2.test.js`
预期:FAIL（无信号量/取消/onUsage）。

- [ ] **Step 3: 实现**

`engine.js` 顶部加信号量 + 改 runStep/runPlan：
```js
function createSemaphore(n) {
  let active = 0; const q = [];
  const acquire = () => new Promise((res) => { const t = () => { if (active < n) { active++; res(); } else q.push(t); }; t(); });
  const release = () => { active--; if (q.length) q.shift()(); };
  return { acquire, release };
}
let SEM = null;
function sem() { const n = Math.max(1, parseInt(process.env.ORCH_CONCURRENCY || '3', 10)); if (!SEM || SEM._n !== n) { SEM = createSemaphore(n); SEM._n = n; } return SEM; }
```
`runStep` 用信号量包裹真正执行,并透传 onChild/onUsage:
```js
async function runStep(step, ctx, prevOutput) {
  const adapter = ctx.adapters[step.agent];
  if (!adapter) throw new Error(`未知 agent: ${step.agent}`);
  const prompt = AUTONOMY + step.prompt.replace('{prev}', (prevOutput || '').slice(-4000));
  const workdir = await ctx.workspace.make(step.id);
  ctx.onStatus(step.id, 'running');
  const s = sem(); await s.acquire();
  let res;
  try {
    res = await adapter.run({
      prompt, workdir,
      onLine: (line) => ctx.onLog(step.id, line),
      onChild: (child) => { ctx.onChild && ctx.onChild(child); },
      onUsage: (u) => { ctx.onUsage && ctx.onUsage(step.id, step.agent, u); },
    });
  } finally { s.release(); }
  ctx.onStatus(step.id, res.success ? 'done' : 'failed');
  return res;
}
```
`runPlan` 的波次里,起每个 step 前检查取消:
```js
    const wave = plan.steps.filter((s) => !started.has(s.id) && ready(s));
    if (wave.length === 0) break;
    if (ctx.isCancelled && ctx.isCancelled()) break; // 取消:不再起新波
    await Promise.all(wave.map(async (s) => {
      started.add(s.id);
      if (ctx.isCancelled && ctx.isCancelled()) { done[s.id] = { output: '', success: false }; return; }
      const prev = s.deps.length ? done[s.deps[0]]?.output : '';
      done[s.id] = s.type === 'loop' ? await runLoop(s, ctx, prev) : await runStep(s, ctx, prev);
    }));
```
runLoop 内 runStep 同样走信号量(因 runStep 已含)。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/engine2.test.js`
预期:PASS,3 用例。再跑 `node --test test/engine.test.js` 确认旧测仍过(echo 不传 onUsage/onChild,可选回调安全)。

- [ ] **Step 5: Commit**

```bash
git add orch/engine.js orch/test/engine2.test.js
git commit -m "feat: engine 并发信号量 + 取消检查 + onChild/onUsage 透传"
```

---

### Task 3: adapters — cli 暴露 child + claude stream-json 解析(文本+usage)

**Files:**
- Modify: `orch/adapters/cli.js`
- Create: `orch/adapters/streamparse.js`
- Modify: `orch/adapters/claude.js`(改专用)、`orch/adapters/generic.js`(inferred 成本)
- Test: `orch/test/streamparse.test.js`

**Interfaces:**
- Produces: `runCli(cmd,args,workdir,onLine,onChild)`;`parseClaudeStream(line)=>{text?,usage?}`;claude adapter 用 stream-json;generic 用 pricing 估算成本经 onUsage。

- [ ] **Step 1: 写失败测试(解析)**

`orch/test/streamparse.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { parseClaudeStream } = require('../adapters/streamparse');

test('提取 assistant 文本', () => {
  const line = JSON.stringify({ type: 'assistant', message: { content: [{ type: 'text', text: '写好了 index.html' }] } });
  assert.equal(parseClaudeStream(line).text, '写好了 index.html');
});
test('提取 result 的 usage 与 cost', () => {
  const line = JSON.stringify({ type: 'result', usage: { input_tokens: 100, output_tokens: 40 }, total_cost_usd: 0.012 });
  const r = parseClaudeStream(line);
  assert.deepEqual(r.usage, { input: 100, output: 40, cost: 0.012 });
});
test('非 JSON 行返回原文本', () => {
  assert.equal(parseClaudeStream('plain log line').text, 'plain log line');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/streamparse.test.js`
预期:FAIL（模块不存在）。

- [ ] **Step 3: 实现 streamparse + cli + claude + generic**

`orch/adapters/streamparse.js`:
```js
// 解析 claude --output-format stream-json 的一行 JSON 事件
function parseClaudeStream(line) {
  let j; try { j = JSON.parse(line); } catch (e) { return { text: line }; } // 非 JSON 当普通文本
  if (j.type === 'assistant' && j.message && Array.isArray(j.message.content)) {
    const t = j.message.content.filter((c) => c.type === 'text').map((c) => c.text).join('');
    return t ? { text: t } : {};
  }
  if (j.type === 'result') {
    const u = j.usage || {};
    return { usage: { input: u.input_tokens || 0, output: u.output_tokens || 0, cost: j.total_cost_usd || 0 }, text: typeof j.result === 'string' ? undefined : undefined };
  }
  return {};
}
module.exports = { parseClaudeStream };
```

`orch/adapters/cli.js`:`runCli` 增 onChild 形参,spawn 后 `onChild && onChild(p)`:
```js
function runCli(cmd, args, workdir, onLine, onChild) {
  return new Promise((resolve) => {
    const p = spawn(cmd, args, { cwd: workdir || process.cwd(), shell: true, stdio: ['ignore', 'pipe', 'pipe'] });
    if (onChild) onChild(p);
    let output = '';
    const onData = (b) => { const s = b.toString(); output += s; s.split('\n').filter(Boolean).forEach(onLine); };
    p.stdout.on('data', onData); p.stderr.on('data', onData);
    p.on('close', (code) => resolve({ output, success: code === 0 }));
    p.on('error', (e) => { onLine(String(e)); resolve({ output: String(e), success: false }); });
  });
}
```

`orch/adapters/claude.js`(改专用,用 stream-json + 行解析):
```js
const { spawn } = require('child_process');
const { parseClaudeStream } = require('./streamparse');

module.exports = {
  run({ prompt, workdir, onLine, onChild, onUsage }) {
    return new Promise((resolve) => {
      const args = ['-p', '--output-format', 'stream-json', '--verbose', '--dangerously-skip-permissions', JSON.stringify(prompt)];
      const p = spawn('claude', args, { cwd: workdir || process.cwd(), shell: true, stdio: ['ignore', 'pipe', 'pipe'] });
      if (onChild) onChild(p);
      let buf = '', output = '';
      const handle = (line) => { if (!line) return; const r = parseClaudeStream(line); if (r.text) { output += r.text + '\n'; onLine(r.text); } if (r.usage && onUsage) onUsage(r.usage); };
      p.stdout.on('data', (b) => { buf += b.toString(); const lines = buf.split('\n'); buf = lines.pop(); lines.forEach(handle); });
      p.stderr.on('data', (b) => onLine(b.toString()));
      p.on('close', (code) => { if (buf) handle(buf); resolve({ output, success: code === 0 }); });
      p.on('error', (e) => { onLine(String(e)); resolve({ output: String(e), success: false }); });
    });
  },
};
```
> claude seed 的 args 不再用于 claude(改专用适配器);server 注册表里 claude 用此专用 adapter,其余用 generic。见 Task 5。

`orch/adapters/generic.js`:run 透传 onChild,并在结束后按 pricing 估算成本 onUsage：
```js
const { runCli } = require('./cli');
function make(def) {
  const args = Array.isArray(def.args) ? def.args : (def.args ? JSON.parse(def.args) : []);
  let pricing = null; try { pricing = def.pricing ? JSON.parse(def.pricing) : null; } catch (e) {}
  return {
    async run({ prompt, workdir, onLine, onChild, onUsage }) {
      const res = await runCli(def.command, [...args, JSON.stringify(prompt)], workdir, onLine, onChild);
      if (onUsage) { // inferred:按字符数/4 估 token
        const inTok = Math.ceil((prompt || '').length / 4), outTok = Math.ceil((res.output || '').length / 4);
        const c = pricing ? (inTok * (pricing.in || 0) + outTok * (pricing.out || 0)) / 1e6 : 0;
        onUsage({ input: inTok, output: outTok, cost: c, estimated: true });
      }
      return res;
    },
  };
}
module.exports = { make };
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/streamparse.test.js`
预期:PASS。再跑 `node --test test/generic.test.js`(generic 仍兼容:onUsage 可选,旧测不传)。

- [ ] **Step 5: Commit**

```bash
git add orch/adapters/cli.js orch/adapters/streamparse.js orch/adapters/claude.js orch/adapters/generic.js orch/test/streamparse.test.js
git commit -m "feat: cli 暴露child + claude stream-json采usage + generic inferred成本"
```

---

### Task 4: runner — 事件落库 + onUsage/onChild + 取消感知

**Files:**
- Modify: `orch/runner.js`
- Test: `orch/test/runner2.test.js`

**Interfaces:**
- Consumes: `deps` 增 `runs`(运行态注册表 Map)。
- Produces: runTask 把 events 落库、usage 落库、注册 children、按 runs[taskId].cancelled 提供 isCancelled;取消时任务标 cancelled。

- [ ] **Step 1: 写失败测试**

`orch/test/runner2.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { open } = require('../store');
const { runTask } = require('../runner');

test('usage 落库 + 事件记录', async () => {
  const store = open(':memory:'); store.seed();
  const id = store.createTask('x', 'p', 'o');
  const a = { async run({ onUsage }) { onUsage({ input: 10, output: 5, cost: 0.001 }); return { output: 'ok', success: true }; } };
  const runs = new Map();
  await runTask(id, { store, adapters: { claude: a }, workspace: { make: () => '.' }, runs, onEvent: () => {}, makePlan: async () => ({ steps: [{ id: 'dev', agent: 'claude', prompt: 'p', deps: [] }] }) });
  assert.equal(store.taskUsage(id).input, 10);
  assert.ok(store.getEvents(id).length >= 1);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/runner2.test.js`
预期:FAIL（usage 未落库）。

- [ ] **Step 3: 实现**

`runner.js` 改 ctx 增 onUsage/onChild/isCancelled,emit 同时落事件:
```js
async function runTask(taskId, deps) {
  const { store, adapters, workspace, onEvent, makePlan, runs } = deps;
  const rec = runs && (runs.get(taskId) || (runs.set(taskId, { cancelled: false, children: new Set() }), runs.get(taskId))) || { cancelled: false, children: new Set() };
  const ev = (type, data) => { if (store.addEvent) store.addEvent(taskId, type, data); emit(onEvent, taskId, null, type === 'plan' ? 'plan' : type, data); };
  store.setTaskStatus(taskId, 'planning');
  const task = store.getTask(taskId);
  const plan = await makePlan(task.text);
  store.setPlan(taskId, plan);
  if (store.addEvent) store.addEvent(taskId, 'plan', { steps: (plan.steps || []).length });
  store.setTaskStatus(taskId, 'running');
  emit(onEvent, taskId, null, 'plan', plan);

  const agentOf = {}; const collect = (steps) => steps.forEach((s) => { if (s.body) collect(s.body); else agentOf[s.id] = s.agent; }); collect(plan.steps);
  const ctx = {
    adapters, workspace,
    isCancelled: () => rec.cancelled,
    onChild: (child) => rec.children.add(child),
    onUsage: (stepId, agent, u) => { store.addUsage(taskId, stepId, agent, u); emit(onEvent, taskId, stepId, 'usage', u); },
    onLog: (stepId, line) => { store.addLog(taskId, stepId, line); emit(onEvent, taskId, stepId, 'log', line, agentOf[stepId]); },
    onStatus: (stepId, status) => { store.setStep(taskId, stepId, agentOf[stepId] || '', status, null); if (store.addEvent) store.addEvent(taskId, 'status', { step: stepId, v: status }); emit(onEvent, taskId, stepId, 'status', status, agentOf[stepId]); },
  };
  try {
    const done = await runPlan(plan, ctx);
    const ok = !rec.cancelled && plan.steps.every((s) => done[s.id] && done[s.id].success);
    const final = rec.cancelled ? 'cancelled' : (ok ? 'done' : 'failed');
    store.setTaskStatus(taskId, final);
    if (store.addEvent) store.addEvent(taskId, 'task', final);
    emit(onEvent, taskId, null, 'task', final);
  } catch (e) {
    store.setTaskStatus(taskId, 'failed');
    emit(onEvent, taskId, null, 'task', 'failed: ' + e.message);
  } finally { if (runs) runs.delete(taskId); }
}
```
emit 签名保留带 agent(Task v2 已是 6 参)。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/runner2.test.js` 与 `node --test test/runner.test.js`
预期:都 PASS。

- [ ] **Step 5: Commit**

```bash
git add orch/runner.js orch/test/runner2.test.js
git commit -m "feat: runner 事件落库 + usage + 取消感知"
```

---

### Task 5: server — 取消端点 + 运行态注册表 + 杀进程 + claude 专用适配器 + usage 广播

**Files:**
- Modify: `orch/server.js`、`orch/api.js`

**Interfaces:**
- Produces: `POST /task/:id/cancel`;`runs` Map 贯通 runTask;杀子进程;buildAll 增 `usage`(今日)与任务/agent 累计;adapters.claude 用专用适配器。

- [ ] **Step 1: 实现**

`server.js`:
- 顶部 `const runs = new Map();` 与 `const claudeAdapter = require('./adapters/claude');`
- `buildAdapters()`:`m.claude = claudeAdapter;` 其余 generic(若 DB 有 claude 定义,仍用专用适配器覆盖)。
```js
function buildAdapters() {
  const m = { echo: require('./adapters/echo') };
  store.listAgents().forEach((a) => { m[a.id] = generic.make(a); });
  m.claude = require('./adapters/claude'); // claude 用 stream-json 专用适配器
  return m;
}
```
- POST /task 传 runs + opts:
```js
app.post('/task', (req, res) => {
  const owner = req.body.user || '操作者'; const project = req.body.project || '默认项目';
  const id = store.createTask(req.body.text, project, owner, { budget: req.body.budget, approve: req.body.approve, isolate: req.body.isolate });
  res.json({ id });
  let dir = ROOT; try { dir = taskDir(ROOT, owner, project, req.body.text, id); } catch (e) {}
  runTask(id, { store, adapters, workspace: { make: () => dir }, runs, makePlan: (text) => makePlan(text, { mode: req.body.mode, agents: store.listAgents().map((a) => a.id), templatesDir, claude: adapters.claude }), onEvent: broadcast });
});
```
- 取消端点(杀进程树):
```js
const { execSync } = require('child_process');
app.post('/task/:id/cancel', (req, res) => {
  const id = Number(req.params.id); const rec = runs.get(id);
  if (rec) {
    rec.cancelled = true;
    rec.children.forEach((c) => { try { if (process.platform === 'win32' && c.pid) execSync('taskkill /T /F /PID ' + c.pid); else c.kill('SIGKILL'); } catch (e) {} });
  }
  store.setTaskStatus(id, 'cancelled'); store.addEvent(id, 'task', 'cancelled'); broadcast({ taskId: id, type: 'task', data: 'cancelled' });
  res.json({ ok: true });
});
```

`api.js`:`buildAll` 返回里加 `usage: store.usageToday()`;tasksVm 每条加 `cost`(store.taskUsage(t.id).cost) 与 token;counts 可加 `costToday`。agents 视图加该 agent 累计成本(可选,简单:从 usage 表 sum by agent)。

- [ ] **Step 2: 联测**

```bash
cd <git目录>; node /d/swap/orch/server.js & sleep 2
# 起一个任务,立刻取消
ID=$(curl -s -X POST localhost:3000/task -H "content-type: application/json" --data '{"text":"sleep 测试","mode":"template","user":"op"}' | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>console.log(JSON.parse(s).id))')
sleep 3; curl -s -X POST localhost:3000/task/$ID/cancel; sleep 1
curl -s localhost:3000/api/all | grep -o '"status":"cancelled"' && echo OK-cancel
curl -s localhost:3000/api/all | grep -o '"usage"' && echo OK-usage
```
预期:OK-cancel(任务被取消)、OK-usage(今日用量字段在)。taskkill 关服务。

- [ ] **Step 3: Commit**

```bash
git add orch/server.js orch/api.js
git commit -m "feat: server 取消端点+杀进程+运行态注册表+claude专用适配器+usage广播"
```

---

### Task 6: 前端 P0 — 停止按钮 + 成本指标 + 取消状态

**Files:** `<scratchpad>/Maestro.html`、`<scratchpad>/wiring.js` → build。

- [ ] **Step 1: 模板**

- 任务详情头部:running 时显示「停止」按钮 `<button onClick="{{ cancelTask }}" ...>停止</button>`(用 `{{ canCancel }}` sc-if 包裹)。
- 任务列表行/任务详情:显示成本 `{{ t.cost }}`(¥/$ + token)。
- 总控台指标:把"今日已完成"旁或新增一格"今日成本 {{ costToday }}"。
- 状态映射:`cancelled` → 文案"已取消",statusMeta 加 `cancelled:{label:'已取消',c:'#6B6760',bg:'#F1EFEA',dot:'#C9C4BA'}`(在 wiring 的 sk 映射处或 api)。tasksVm 的 taskSk 加 `cancelled:'cancelled'`,前端 statusMeta 加该键(基类 statusMeta 没有 → 在 wiring 覆盖 statusMeta 或 api 直接给 sLabel)。简单做法:api.taskSk 映射 `cancelled→'idle'` 并把 label 显示"已取消"——用 isolate? 直接在 api 给 tasksVm 加 sk:'cancelled' 并在前端 statusMeta 补。

- [ ] **Step 2: 逻辑(wiring.js)**

```js
  cancelTask() { const id = this.state.taskId; if (typeof id !== 'number') return; fetch('/task/' + id + '/cancel', { method: 'POST' }).then(() => this.fetchAll()).catch(() => {}); }
```
renderVals 追加:
```js
    v.cancelTask = () => this.cancelTask();
    const ct = this.live.counts || {};
    v.costToday = (this.live.usage && ('$' + (this.live.usage.cost || 0).toFixed(3))) || '$0';
    // 当前任务可取消?(真实任务 + running)
    const curT = typeof this.state.taskId === 'number' && this.TASKS.find((t) => t.id === this.state.taskId);
    v.canCancel = !!(curT && curT.sk === 'working');
```
fetchAll 里存 `this.live.usage = d.usage`。tasksVm 的 cost 由 api 提供→直接渲染。statusMeta 补 cancelled:基类 statusMeta 无此键,wiring 加覆盖:
```js
  statusMeta(s) { if (s === 'cancelled') return { label: '已取消', c: '#6B6760', bg: '#F1EFEA', dot: '#C9C4BA' }; return super.statusMeta(s); }
```

- [ ] **Step 3: 重建 + 冒烟**

`node build.js`;重启;起任务→详情点「停止」→转"已取消";总控台显示今日成本;任务详情显示 token/¥。

- [ ] **Step 4: Commit**

```bash
git add orch/web/index.html orch/web/app.js
git commit -m "feat: 前端 停止任务 + 成本指标 + 已取消状态"
```

---

## 阶段 P1

### Task 7: 预算熔断 + 审批门 + 可编辑计划

**Files:** `orch/runner.js`、`orch/server.js`、`<scratchpad>/Maestro.html`/`wiring.js`、`orch/test/runner3.test.js`

**Interfaces:**
- Produces: 任务累计 token>budget 自动取消;approve=1 时出 plan 后暂停待 `POST /task/:id/approve {plan?}`。

- [ ] **Step 1: 写失败测试(预算 + 审批)**

`orch/test/runner3.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { open } = require('../store');
const { runTask } = require('../runner');

test('超预算自动取消', async () => {
  const store = open(':memory:'); store.seed();
  const id = store.createTask('x', 'p', 'o', { budget: 8 });
  const a = { async run({ onUsage }) { onUsage({ input: 10, output: 5, cost: 0.001 }); return { output: 'ok', success: true }; } };
  const runs = new Map();
  await runTask(id, { store, adapters: { claude: a }, workspace: { make: () => '.' }, runs, onEvent: () => {}, makePlan: async () => ({ steps: [{ id: 'a', agent: 'claude', prompt: 'p', deps: [] }, { id: 'b', agent: 'claude', prompt: 'p', deps: ['a'] }] }) });
  // 第一步用 15 token 已超 8 → 取消 → b 不应跑;任务 cancelled
  assert.equal(store.listTasks().find((t) => t.id === id).status, 'cancelled');
});

test('审批模式出plan后不执行', async () => {
  const store = open(':memory:'); store.seed();
  const id = store.createTask('x', 'p', 'o', { approve: 1 });
  let ran = 0; const a = { async run() { ran++; return { output: '', success: true }; } };
  const runs = new Map();
  await runTask(id, { store, adapters: { claude: a }, workspace: { make: () => '.' }, runs, onEvent: () => {}, makePlan: async () => ({ steps: [{ id: 'a', agent: 'claude', prompt: 'p', deps: [] }] }) });
  assert.equal(ran, 0);
  assert.equal(store.listTasks().find((t) => t.id === id).status, 'awaiting');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/runner3.test.js`
预期:FAIL。

- [ ] **Step 3: 实现**

`runner.js`:
- 审批:加载 task 后,出 plan 存库;若 `task.approve` → `setTaskStatus(id,'awaiting')` + 事件 + return(不执行)。新增导出 `runApproved(taskId, deps, plan)` 供 approve 端点调用(用给定 plan 执行,逻辑同现执行段)。重构:把"执行 plan"抽成内部函数 `execute(taskId, plan, deps, rec)`,runTask 在非审批时调用,approve 端点也调用。
- 预算:onUsage 回调里累加后检查 `if (task.budget>0 && store.taskUsage(taskId).input+store.taskUsage(taskId).output > task.budget) { rec.cancelled=true; ... 杀children }`。因 server 持有 children 杀逻辑,简单做法:onUsage 里置 rec.cancelled=true 并标记需取消;engine 下个 step 前 isCancelled 为真即停。子进程杀由 server 端点负责;预算熔断在 runner 内置 rec.cancelled + 记事件"预算超限",最终 final='cancelled'。(已 spawn 的当前 step 自然跑完,不强杀,够用。)

`server.js`:
```js
app.post('/task/:id/approve', (req, res) => {
  const id = Number(req.params.id); const t = store.getTask(id); if (!t) return res.json({ ok: false });
  let plan = req.body && req.body.plan; if (!plan) { try { plan = JSON.parse(t.plan); } catch (e) { plan = { steps: [] }; } }
  res.json({ ok: true });
  require('./runner').runApproved(id, { store, adapters, workspace: { make: () => taskDirFor(t) }, runs, onEvent: broadcast }, plan);
});
```
(taskDirFor:据 t.owner/project/text/id 重建 dir;抽个小辅助。)

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/runner3.test.js`
预期:PASS。

- [ ] **Step 5: 前端**

- 新建任务弹窗加:预算输入 `id="nt-budget"`(token,选填)、审批勾选 `id="nt-approve"`、隔离下拉 `id="nt-isolate"`(none/worktree/container)。submitTask 带上 budget/approve/isolate。
- 编排画布"智能编排":task.status==='awaiting' 时,执行计划每步可编辑(agent 下拉/prompt 输入/删除),「批准并运行」onClick `{{ approveTask }}` 收集编辑后 plan POST /approve;「取消」onClick cancelTask。
- wiring:`approveTask()` 读编辑后的步骤(从 DOM 或维护 state.editPlan)组 plan POST。简化:第一版只支持"原样批准"(不改),把现有 plan POST;编辑作为 stretch(若 DOM 收集复杂,先上原样批准 + 删除步骤两种操作)。

- [ ] **Step 6: Commit**

```bash
git add orch/runner.js orch/server.js orch/web orch/test/runner3.test.js
git commit -m "feat: 预算熔断 + 审批门 + 计划可批准"
```

---

## 阶段 P2

### Task 8: 隔离 — git worktree + opt-in 容器(自定义 agent)

**Files:** `orch/workspace.js`、`orch/server.js`、`orch/adapters/generic.js`、`orch/test/workspace3.test.js`、`orch/README.md`

**Interfaces:**
- Produces: `worktreeDir(root, id)`(git 仓内建 worktree);server 按 task.isolate 选 workspace;generic 在 isolate==='container'+有 image+docker 可用时走 `docker run`。

- [ ] **Step 1: 写失败测试(worktree + docker 命令构造)**

`orch/test/workspace3.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const fs = require('fs'); const os = require('os'); const path = require('path'); const { execSync } = require('child_process');
const { worktreeDir, dockerArgs } = require('../workspace');

test('worktree 在 git 仓内建独立分支目录', () => {
  const repo = fs.mkdtempSync(path.join(os.tmpdir(), 'wt-'));
  execSync('git init -q', { cwd: repo }); fs.writeFileSync(path.join(repo, 'a.txt'), 'x');
  execSync('git add . && git -c user.name=t -c user.email=t@t commit -q -m s', { cwd: repo });
  const dir = worktreeDir(repo, 9);
  assert.ok(fs.existsSync(dir)); assert.ok(fs.existsSync(path.join(dir, 'a.txt')));
});
test('dockerArgs 构造挂载命令', () => {
  const a = dockerArgs('/work/dir', 'node:20', 'node', ['-e', 'x']);
  assert.deepEqual(a.slice(0, 3), ['run', '--rm', '-v']);
  assert.ok(a.includes('node:20'));
});
```

- [ ] **Step 2: 跑测试确认失败 → 实现 workspace**

`orch/workspace.js` 追加:
```js
const { execSync } = require('child_process');
function worktreeDir(root, id) {
  const dir = path.join(root, 'worktrees', 'task-' + id);
  if (!fs.existsSync(dir)) { try { execSync('git worktree add -B orch/task-' + id + ' "' + dir + '"', { cwd: root }); } catch (e) { fs.mkdirSync(dir, { recursive: true }); } }
  return dir;
}
function dockerArgs(mountDir, image, cmd, args) {
  return ['run', '--rm', '-v', mountDir + ':/work', '-w', '/work', image, cmd, ...args];
}
module.exports = { makeWorkspace, slug, taskDir, worktreeDir, dockerArgs };
```

- [ ] **Step 3: 实现 server 选 workspace + generic 容器分支**

`server.js` POST /task 选目录:
```js
function isGit(d){ try{ require('child_process').execSync('git rev-parse --is-inside-work-tree',{cwd:d,stdio:'ignore'}); return true;}catch(e){return false;} }
// dir 选择:
let dir = ROOT;
if (req.body.isolate === 'worktree' && isGit(ROOT)) { try { dir = worktreeDir(ROOT, id); } catch(e){ dir = taskDir(ROOT, owner, project, req.body.text, id); } }
else { try { dir = taskDir(ROOT, owner, project, req.body.text, id); } catch (e) {} }
```
`generic.js` run:若 `def.isolate==='container'`(由 server 在 make 时把 task.isolate 传入?简化:容器隔离按 agent.image 存在 + 一个全局 env `ORCH_CONTAINER` 或 task 标记)。**简化范围**:generic.make(def) 时若 `def.image` 存在则用 `docker run`(dockerArgs)替代直接命令,docker 不可用回退本地 onLine 告警:
```js
const { runCli } = require('./cli'); const { dockerArgs } = require('../workspace');
function dockerOk(){ try{ require('child_process').execSync('docker --version',{stdio:'ignore'}); return true;}catch(e){return false;} }
// run 内:
let cmd = def.command, callArgs = [...args, JSON.stringify(prompt)];
if (def.image) { if (dockerOk()) { callArgs = dockerArgs(workdir, def.image, def.command, [...args, JSON.stringify(prompt)]); cmd = 'docker'; } else onLine('[warn] Docker 不可用,回退本地执行'); }
const res = await runCli(cmd, callArgs, workdir, onLine, onChild);
```
> claude/codex 无 image → 永远本地,符合诚实范围。

- [ ] **Step 4: 跑测试确认通过 + README**

Run: `cd orch && node --test test/workspace3.test.js`
README 增:隔离三档(none/worktree/container)说明 + 容器仅对设了 image 的自定义 agent 生效、claude/codex 始终本地的限制。

- [ ] **Step 5: Commit**

```bash
git add orch/workspace.js orch/server.js orch/adapters/generic.js orch/test/workspace3.test.js orch/README.md
git commit -m "feat: git worktree 隔离 + opt-in 容器(自定义agent,docker可用则用)"
```

---

### Task 9: 端到端 + README 总结

- [ ] **Step 1: 全量回归** `cd orch && npm test` → 全 PASS。
- [ ] **Step 2: 真跑验证**:起服务;(a)起任务中途「停止」→ 进程被杀、转已取消;(b)跑完看 token/成本显示;(c)审批模式任务停在 awaiting、批准后执行;(d)并发设 ORCH_CONCURRENCY=1 起多任务观察排队。
- [ ] **Step 3: README** 汇总 v4 新功能。
- [ ] **Step 4: Commit** `docs: orch v4 使用说明`。

---

## 自检结果

- **Spec 覆盖**:P0-1 取消/事件=T1/T4/T5/T6;P0-2 并发=T2;P0-3 成本=T1/T3/T4/T5/T6;P1-1 预算=T7;P1-2 审批/编辑=T7;P2-1 worktree=T8;P2-2 容器=T8。全覆盖。
- **占位符**:无 TBD;关键代码(信号量/取消/stream-json/usage/worktree/docker)均给出完整实现。审批的 plan 可编辑前端标注了"第一版原样批准+删步,完整拖拽编辑为 stretch"——为有界简化,非占位。
- **类型一致**:adapter `run({prompt,workdir,onLine,onChild,onUsage})`、`onUsage(u={input,output,cost})`、`parseClaudeStream`、store `addEvent/getEvents/addUsage/taskUsage/usageToday`、`createTask(...,opts)`、`worktreeDir/dockerArgs`、runs Map 形状 前后一致。
- **风险**:1) claude `--output-format stream-json` 实际事件结构需以真实输出核对(Task3 解析按官方 schema,落地时先跑一条真命令验证字段名);2) 取消杀进程树在 Windows 用 taskkill /T,已处理;3) 容器隔离对 claude/codex 不适用,已诚实限定。
