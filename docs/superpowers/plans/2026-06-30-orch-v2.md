# orch v2 实现计划 — 可配置 Agent + 人员 + LLM 深度编排

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 orch 支持真下发(含 LLM 深度拆解)、数据驱动可新建 agent、人员 CRUD+分配、每 agent 实时控制台。

**Architecture:** agent/人员落 SQLite,适配器注册表从库动态构建;planner 增 LLM 模式(注入可用 agent + 校验 + 回退模板);引擎不变(已支持 DAG/loop/并发);前端加弹窗与每 agent 直播控制台。

**Tech Stack:** Node 18+,express,ws,better-sqlite3,js-yaml,内置 `node --test`。前端原生 JS + dc 运行时(已有)。

## Global Constraints

- 依赖只允许:express、ws、better-sqlite3、js-yaml。不新增依赖、不引前端框架/测试框架。
- 平台 Windows:spawn 一律 `shell:true`;路径 `path.join`。
- Agent 适配器接口不变:`run({prompt,workdir,onLine}) => Promise<{output,success}>`。
- 前端通过已有 `runtime.js`(RT.Component + morph)渲染;morph 保留 DOM 节点,表单用非受控 input + `getElementById` 读值。
- 安全:自定义 agent 的自主模式参数由用户在 args 自填;不额外注入。

---

### Task 1: store 增 agents / people / 分配 表 + seed

**Files:**
- Modify: `orch/store.js`
- Test: `orch/test/store2.test.js`

**Interfaces:**
- Produces:
  - `listAgents() => [{id,name,command,args,model,caps,color,avatar,dept}]`(args/caps 为 JSON 字符串原样)
  - `addAgent(def) => id`(def.id 缺省时由 name 派生 slug)
  - `listPeople() => [{id,name,email,role,color,av}]`
  - `addPerson(def) => id`
  - `setPersonAgents(pid, agentIds)` / `listPersonAgents(pid) => [agentId]`
  - `seed()`(无 agents 时插 claude/codex;无 people 时插操作者)

- [ ] **Step 1: 写失败测试**

`orch/test/store2.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { open } = require('../store');

test('seed 后有 claude/codex 两个 agent', () => {
  const s = open(':memory:'); s.seed();
  const ids = s.listAgents().map((a) => a.id).sort();
  assert.deepEqual(ids, ['claude', 'codex']);
});

test('新建 agent 可查到', () => {
  const s = open(':memory:'); s.seed();
  const id = s.addAgent({ name: 'Gemini 开发', command: 'gemini', args: ['-p'], caps: ['代码'], dept: 'dev' });
  assert.ok(s.listAgents().some((a) => a.id === id));
});

test('人员与分配', () => {
  const s = open(':memory:'); s.seed();
  const pid = s.addPerson({ name: '李四', role: '开发' });
  s.setPersonAgents(pid, ['claude', 'codex']);
  assert.deepEqual(s.listPersonAgents(pid).sort(), ['claude', 'codex']);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/store2.test.js`
预期:FAIL（`s.seed is not a function`）。

- [ ] **Step 3: 实现**

在 `orch/store.js` 的 `db.exec(...)` 建表语句里追加（与现有 tasks/steps/logs 并列）:
```js
    CREATE TABLE IF NOT EXISTS agents(
      id TEXT PRIMARY KEY, name TEXT, command TEXT, args TEXT,
      model TEXT, caps TEXT, color TEXT, avatar TEXT, dept TEXT);
    CREATE TABLE IF NOT EXISTS people(
      id TEXT PRIMARY KEY, name TEXT, email TEXT, role TEXT, color TEXT, av TEXT);
    CREATE TABLE IF NOT EXISTS person_agents(
      person_id TEXT, agent_id TEXT, PRIMARY KEY(person_id, agent_id));
```

在 `return { ... }` 对象里追加方法（放在 `db,` 之前）:
```js
    listAgents() { return db.prepare('SELECT * FROM agents').all(); },
    addAgent(d) {
      const id = d.id || (String(d.name || 'agent').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '') || 'agent') + '-' + (db.prepare('SELECT COUNT(*) n FROM agents').get().n + 1);
      db.prepare('INSERT OR REPLACE INTO agents(id,name,command,args,model,caps,color,avatar,dept) VALUES(?,?,?,?,?,?,?,?,?)')
        .run(id, d.name || id, d.command || '', JSON.stringify(d.args || []), d.model || '—', JSON.stringify(d.caps || []), d.color || '#7C6FD9', d.avatar || (d.name || 'A').slice(0, 1).toUpperCase(), d.dept || 'dev');
      return id;
    },
    listPeople() { return db.prepare('SELECT * FROM people').all(); },
    addPerson(d) {
      const id = d.id || 'p-' + (db.prepare('SELECT COUNT(*) n FROM people').get().n + 1);
      db.prepare('INSERT OR REPLACE INTO people(id,name,email,role,color,av) VALUES(?,?,?,?,?,?)')
        .run(id, d.name || id, d.email || '', d.role || '成员', d.color || '#E0922E', (d.name || '人').slice(0, 1).toUpperCase());
      return id;
    },
    setPersonAgents(pid, ids) {
      db.prepare('DELETE FROM person_agents WHERE person_id=?').run(pid);
      const ins = db.prepare('INSERT OR IGNORE INTO person_agents(person_id,agent_id) VALUES(?,?)');
      (ids || []).forEach((a) => ins.run(pid, a));
    },
    listPersonAgents(pid) { return db.prepare('SELECT agent_id FROM person_agents WHERE person_id=?').all(pid).map((r) => r.agent_id); },
    seed() {
      if (db.prepare('SELECT COUNT(*) n FROM agents').get().n === 0) {
        this.addAgent({ id: 'claude', name: 'Claude', command: 'claude', args: ['-p', '--dangerously-skip-permissions'], model: 'claude CLI', caps: ['代码生成', '重构', '单元测试'], color: '#7C6FD9', avatar: 'C', dept: 'dev' });
        this.addAgent({ id: 'codex', name: 'Codex', command: 'codex', args: ['exec', '--dangerously-bypass-approvals-and-sandbox', '--skip-git-repo-check'], model: 'codex CLI', caps: ['功能验证', '回归测试', '沙箱执行'], color: '#4F8BE8', avatar: 'X', dept: 'qa' });
      }
      if (db.prepare('SELECT COUNT(*) n FROM people').get().n === 0) {
        const op = process.env.USERNAME || process.env.USER || 'operator';
        this.addPerson({ id: 'op', name: op, role: '操作者', email: op + '@local' });
      }
    },
```
> `addAgent` 用 `this.addAgent` 须保证返回对象方法间可互调:`seed` 内用 `this.addAgent`/`this.addPerson`,因返回的是对象字面量,`this` 在方法调用时即该对象,成立。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/store2.test.js`
预期:PASS,3 个用例。

- [ ] **Step 5: Commit**

```bash
git add orch/store.js orch/test/store2.test.js
git commit -m "feat: store 增 agents/people/分配 表 + seed"
```

---

### Task 2: 通用 CLI 适配器(数据驱动)

**Files:**
- Create: `orch/adapters/generic.js`
- Test: `orch/test/generic.test.js`

**Interfaces:**
- Consumes: `adapters/cli.js` 的 `runCli`。
- Produces: `make(def) => { run({prompt,workdir,onLine}) }`，`def` 含 `command`、`args`(数组或 JSON 字符串)。

- [ ] **Step 1: 写失败测试**

`orch/test/generic.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const generic = require('../adapters/generic');

test('按 command+args 跑,退出码0=成功', async () => {
  const a = generic.make({ command: 'node', args: ['-e', 'console.log(1+1)'] });
  const lines = [];
  const r = await a.run({ prompt: '忽略', workdir: process.cwd(), onLine: (l) => lines.push(l) });
  assert.equal(r.success, true);
  assert.ok(lines.join('').includes('2'));
});

test('args 接受 JSON 字符串', async () => {
  const a = generic.make({ command: 'node', args: '["-e","process.exit(0)"]' });
  const r = await a.run({ prompt: 'x', workdir: process.cwd(), onLine: () => {} });
  assert.equal(r.success, true);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/generic.test.js`
预期:FAIL（模块找不到）。

- [ ] **Step 3: 实现**

`orch/adapters/generic.js`:
```js
const { runCli } = require('./cli');

// 数据驱动:按 agent 定义的 command + args 跑,prompt 作为末尾参数(加引号防空格拆分)。
function make(def) {
  const args = Array.isArray(def.args) ? def.args : (def.args ? JSON.parse(def.args) : []);
  return {
    run({ prompt, workdir, onLine }) {
      return runCli(def.command, [...args, JSON.stringify(prompt)], workdir, onLine);
    },
  };
}

module.exports = { make };
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/generic.test.js`
预期:PASS。

- [ ] **Step 5: Commit**

```bash
git add orch/adapters/generic.js orch/test/generic.test.js
git commit -m "feat: 数据驱动通用 CLI 适配器"
```

---

### Task 3: planner 增 LLM 模式(注入可用 agent + 校验 + 回退)

**Files:**
- Modify: `orch/planner.js`
- Test: `orch/test/planner2.test.js`

**Interfaces:**
- Consumes: claude 适配器(LLM 拆解)。
- Produces: `makePlan(text, { mode, agents, templatesDir, claude }) => Promise<plan>`；`validate(plan, agentIds) => bool`。

- [ ] **Step 1: 写失败测试**

`orch/test/planner2.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const path = require('path');
const { makePlan, validate } = require('../planner');
const TPL = path.join(__dirname, '..', 'templates');

test('合法 plan 校验通过', () => {
  const ok = validate({ steps: [{ id: 'a', agent: 'claude', prompt: 'p', deps: [] }] }, ['claude', 'codex']);
  assert.equal(ok, true);
});
test('agent 不在可用列表则不通过', () => {
  assert.equal(validate({ steps: [{ id: 'a', agent: 'ghost', prompt: 'p', deps: [] }] }, ['claude']), false);
});
test('LLM 模式:合法 JSON 直接用', async () => {
  const claude = { async run() { return { output: '```json\n{"steps":[{"id":"x","agent":"claude","prompt":"p","deps":[]}]}\n```', success: true }; } };
  const plan = await makePlan('做事', { mode: 'llm', agents: ['claude', 'codex'], templatesDir: TPL, claude });
  assert.equal(plan.steps[0].id, 'x');
});
test('LLM 模式:非法(未知agent)回退模板', async () => {
  const claude = { async run() { return { output: '{"steps":[{"id":"x","agent":"ghost","prompt":"p","deps":[]}]}', success: true }; } };
  const plan = await makePlan('做事', { mode: 'llm', agents: ['claude', 'codex'], templatesDir: TPL, claude });
  assert.equal(plan.steps[0].id, 'dev'); // 兜底模板首步
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/planner2.test.js`
预期:FAIL（`validate` 未导出 / makePlan 签名不符）。

- [ ] **Step 3: 实现**

修改 `orch/planner.js`。把 `fromLLM` 改为接收 agentIds 并改造 `makePlan`,新增 `validate`。

将现有 `fromLLM` 函数整体替换为:
```js
function collectIds(steps, acc) {
  steps.forEach((s) => { acc.push(s.id); if (s.body) collectIds(s.body, acc); });
  return acc;
}
function validate(plan, agentIds) {
  if (!plan || !Array.isArray(plan.steps) || !plan.steps.length) return false;
  const ids = collectIds(plan.steps, []);
  const checkStep = (s) => {
    if (s.type === 'loop') return Array.isArray(s.body) && s.body.length && s.body.every(checkStep);
    if (!agentIds.includes(s.agent)) return false;
    if (s.deps && s.deps.some((d) => !ids.includes(d))) return false;
    return true;
  };
  return plan.steps.every(checkStep);
}
async function fromLLM(text, claude, agentIds) {
  const prompt = `把下面的开发任务拆成 JSON,字段 steps,每步 {id,agent,prompt,deps}。`
    + `agent 只能取这些 id 之一: ${agentIds.join(', ')}。`
    + `可用 {id,type:"loop",until:"pass",max,deps,body:[...]} 表示开发↔验证改测循环。`
    + `多个无依赖的步骤会并行。只输出 JSON,不要解释。任务: ${text}`;
  const { output } = await claude.run({ prompt, workdir: process.cwd(), onLine: () => {} });
  const plan = extractJson(output);
  plan.task = text;
  return plan;
}
```

将 `makePlan` 替换为:
```js
async function makePlan(text, opts) {
  const { mode, agents, templatesDir, claude } = opts;
  if (mode === 'llm' && claude && agents && agents.length) {
    try {
      const plan = await fromLLM(text, claude, agents);
      if (validate(plan, agents)) return plan;
    } catch (e) { /* 落到模板 */ }
  }
  return fromTemplate(text, templatesDir) || { task: text, steps: [] };
}
```

更新导出:
```js
module.exports = { fromTemplate, fromLLM, makePlan, validate };
```
> `extractJson` 与 `fromTemplate` 保持原样不动。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/planner2.test.js`
预期:PASS,4 个用例。

- [ ] **Step 5: 回归 + Commit**

Run: `cd orch && node --test test/planner.test.js`（旧测试:`makePlan('任意', {templatesDir, claude})` 无 mode → 走模板,仍通过）
```bash
git add orch/planner.js orch/test/planner2.test.js
git commit -m "feat: planner LLM 深度拆解(注入agent+校验+回退)"
```

---

### Task 4: runner 的 log/status 事件带 agent 字段

**Files:**
- Modify: `orch/runner.js`
- Test: `orch/test/runner.test.js`（更新）

**Interfaces:**
- Produces: `onEvent` 收到的 log/status 事件含 `agent` 字段（供前端每 agent 控制台过滤）。

- [ ] **Step 1: 更新测试**

把 `orch/test/runner.test.js` 里的 `onEvent: () => {}` 改为收集事件并断言 log 带 agent。替换该测试为:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { open } = require('../store');
const { runTask } = require('../runner');

test('runTask 落库且 log 事件带 agent', async () => {
  const store = open(':memory:');
  const id = store.createTask('随便');
  const echo = { async run({ prompt, onLine }) { onLine(prompt); return { output: prompt, success: true }; } };
  const evs = [];
  await runTask(id, {
    store, adapters: { claude: echo, codex: echo },
    workspace: { make: () => '.', merge: () => {} },
    onEvent: (e) => evs.push(e),
    makePlan: async () => ({ task: 'x', steps: [{ id: 'dev', agent: 'claude', prompt: 'p', deps: [] }] }),
  });
  assert.equal(store.getTask(id).status, 'done');
  const log = evs.find((e) => e.type === 'log');
  assert.equal(log.agent, 'claude');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/runner.test.js`
预期:FAIL（log.agent undefined）。

- [ ] **Step 3: 实现**

`orch/runner.js`:把 `emit` 改为带 agent，并在 onLog/onStatus 传入 agent。

将 `emit` 函数替换为:
```js
function emit(onEvent, taskId, stepId, type, data, agent) {
  if (onEvent) onEvent({ taskId, stepId, type, data, agent });
}
```

将 ctx 的 onLog/onStatus 改为:
```js
    onLog: (stepId, line) => {
      store.addLog(taskId, stepId, line);
      emit(onEvent, taskId, stepId, 'log', line, agentOf[stepId]);
    },
    onStatus: (stepId, status) => {
      store.setStep(taskId, stepId, agentOf[stepId] || '', status, null);
      emit(onEvent, taskId, stepId, 'status', status, agentOf[stepId]);
    },
```
> `agentOf` 映射已在文件中构建。`plan`/`task` 两类 emit 不带 agent（保持原 4 参调用即可,agent 为 undefined)。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/runner.test.js`
预期:PASS。

- [ ] **Step 5: Commit**

```bash
git add orch/runner.js orch/test/runner.test.js
git commit -m "feat: runner log/status 事件带 agent 字段"
```

---

### Task 5: api.js 改为读 DB 的 agents/people/depts

**Files:**
- Modify: `orch/api.js`

**Interfaces:**
- Consumes: `store.listAgents()`、`store.listPeople()`、`store.listPersonAgents()`。
- Produces: `buildAll(store)` 的 agents/depts/people 来自 DB;`relay`/`plan` 的 agent 颜色/标签从 DB 查;新增 `ROLEof(store)` 内部辅助。

- [ ] **Step 1: 实现(无独立单测,靠 Task 8 e2e + 现有 store 测试)**

修改 `orch/api.js`:把写死的 `ROLE` 常量改为从 DB 构建的查找表，并让 depts 从 agent.dept 派生。

将顶部 `const ROLE = {...}` 替换为按需构建:
```js
const DEPT_META = {
  dev: { name: '开发部', glyph: '</>', color: '#7C6FD9', soft: 'rgba(124,111,217,.2)', desc: '编写与重构代码' },
  qa: { name: '测试 / QA 部', glyph: '✓', color: '#4F8BE8', soft: 'rgba(79,139,232,.2)', desc: '功能验证与回归' },
};
function roleMap(store) {
  const m = {};
  store.listAgents().forEach((a) => {
    m[a.id] = { dept: a.dept || 'dev', label: a.name, model: a.model, color: a.color, av: a.avatar, caps: (() => { try { return JSON.parse(a.caps); } catch (e) { return []; } })() };
  });
  return m;
}
```

`buildAll(store)` 开头加 `const ROLE = roleMap(store);`,其余对 `ROLE` 的引用照旧（`ROLE[id]`、`Object.keys(ROLE)`）。

`agents` 改为遍历 `Object.keys(ROLE)`（已是如此）。`DEPT_DEFS` 改为从 ROLE 派生:
```js
  const deptIds = [...new Set(Object.values(ROLE).map((r) => r.dept))];
  const DEPT_DEFS = deptIds.map((id) => {
    const meta = DEPT_META[id] || { name: id, glyph: '·', color: '#7C6FD9', soft: 'rgba(124,111,217,.2)', desc: '' };
    const agent = Object.keys(ROLE).find((aid) => ROLE[aid].dept === id);
    return { id, agent, ...meta };
  });
```
> 原 `DEPT_DEFS`(模块级常量)删除,改为 buildAll 内按 ROLE 派生（每个 dept 取该 dept 第一个 agent 作 board 代表;若一个 dept 多 agent,board 仅示首个——本地工具够用）。

`people` 改为从 DB:
```js
  const people = store.listPeople().map((p) => {
    const assigned = store.listPersonAgents(p.id);
    return { id: p.id, name: p.name, av: p.av, color: p.color, role: p.role, email: p.email, projects: Object.keys(projMap).length, agents: assigned.length, assignedIds: assigned, last: rel(lastTs) };
  });
```
（删除原先写死的单操作者 `people`。）

`relay`/`plan`/`agentLog` 顶部各加 `const ROLE = roleMap(store);`，其余引用 `ROLE[...]` 不变。导出新增 `roleMap`:
```js
module.exports = { buildAll, relay, plan, agentLog, roleMap };
```
> 注意:server 的 `toActivity` 用了 `api.ROLE`——改为 `api.roleMap(store)[agent]` 查,见 Task 6。

- [ ] **Step 2: 冒烟验证(空库)**

Run（先确保 server 能起，见 Task 6 后联测）：本任务仅改 api.js,语法自检:
`cd orch && node -e "require('./api'); console.log('api ok')"`
预期:打印 `api ok`,无语法错误。

- [ ] **Step 3: Commit**

```bash
git add orch/api.js
git commit -m "feat: api 的 agents/部门/人员 改为读 DB"
```

---

### Task 6: server 端点 + 适配器注册表 + mode

**Files:**
- Modify: `orch/server.js`

**Interfaces:**
- Produces: `POST /api/agents`、`GET/POST /api/people`、`POST /api/people/:id/agents`;适配器注册表从 DB 构建并可刷新;`POST /task` 收 `mode`;`toActivity` 用 `roleMap`。

- [ ] **Step 1: 实现**

`orch/server.js` 修改:

引入 generic + seed,把适配器注册表改为可重建:
```js
const generic = require('./adapters/generic');
// ...existing requires...
store.seed();

function buildAdapters() {
  const m = { echo: require('./adapters/echo') };
  store.listAgents().forEach((a) => { m[a.id] = generic.make(a); });
  return m;
}
let adapters = buildAdapters();
```
（删除原先写死的 `const adapters = { claude:..., codex:... }`。）

新增端点（放在现有 `/api/*` 路由附近）:
```js
app.post('/api/agents', (req, res) => {
  const id = store.addAgent(req.body || {});
  adapters = buildAdapters();
  broadcastRaw({ type: 'agents' });
  res.json({ id });
});
app.post('/api/people', (req, res) => res.json({ id: store.addPerson(req.body || {}) }));
app.post('/api/people/:id/agents', (req, res) => { store.setPersonAgents(req.params.id, (req.body || {}).agentIds || []); res.json({ ok: true }); });
```

`POST /task` 改为收 mode + 注入可用 agent:
```js
app.post('/task', (req, res) => {
  const id = store.createTask(req.body.text, req.body.project);
  res.json({ id });
  runTask(id, {
    store, adapters, workspace,
    makePlan: (text) => makePlan(text, { mode: req.body.mode, agents: store.listAgents().map((a) => a.id), templatesDir, claude: adapters.claude }),
    onEvent: broadcast,
  });
});
```

`toActivity` 里 `api.ROLE[...]` 改为:
```js
  const role = st && api.roleMap(store)[st.agent];
```

- [ ] **Step 2: 联测(起服务 + curl)**

```bash
cd /c/.../scratchpad/target2   # 任意 git 目录
node /d/swap/orch/server.js & sleep 2
curl -s localhost:3000/api/all | grep -o '"agents":\[' && echo OK-agents
curl -s -X POST localhost:3000/api/agents -H "content-type: application/json" -d '{"name":"Gemini","command":"gemini","args":["-p"]}'
curl -s localhost:3000/api/all | grep -o gemini && echo OK-newagent
curl -s -X POST localhost:3000/api/people -H "content-type: application/json" -d '{"name":"李四","role":"开发"}'
```
预期:OK-agents、新建后 OK-newagent、人员创建返回 id。完事 `taskkill` 关掉。

- [ ] **Step 3: Commit**

```bash
git add orch/server.js
git commit -m "feat: server agents/people 端点 + 动态适配器注册表 + 下发mode"
```

---

### Task 7: 前端 — 弹窗 + 接线死按钮 + 每 agent 实时控制台

**Files:**
- Modify: 设计源 `<scratchpad>/Maestro.html`（模板）与 `<scratchpad>/wiring.js`（逻辑），再用 `build.js` 重建到 `orch/web/index.html`、`orch/web/app.js`。

**Interfaces:**
- Consumes: `/api/all`、`/api/agents`、`/api/people`、`/api/people/:id/agents`、`POST /task`(带 mode)、WS log 事件(带 agent)。
- Produces: 三个弹窗(新建任务/新建Agent/新建人员+分配)、画布与智能编排按钮可下发、agent 详情页实时控制台。

- [ ] **Step 1: 模板加弹窗 + 接线按钮(编辑 Maestro.html)**

在 `</x-dc>` 前(主 div 内末尾)加弹窗 overlay（用 sc-if 控制显隐;非受控 input 带 id）:
```html
  <sc-if value="{{ modalTask }}">
  <div style="position:fixed;inset:0;background:rgba(20,18,14,.4);display:flex;align-items:center;justify-content:center;z-index:50;">
    <div style="background:#fff;border-radius:16px;padding:22px;width:480px;box-shadow:0 20px 60px rgba(0,0,0,.25);">
      <div style="font-size:16px;font-weight:700;margin-bottom:14px;">新建任务</div>
      <textarea id="nt-text" placeholder="描述目标,例如:开发一个电商公司官网首页" style="width:100%;height:90px;border:1px solid #E6E3DC;border-radius:9px;padding:10px;font-family:inherit;font-size:13px;resize:vertical;"></textarea>
      <input id="nt-proj" placeholder="项目(可选)" style="width:100%;margin-top:10px;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;font-size:13px;">
      <div style="display:flex;gap:14px;margin-top:12px;font-size:13px;">
        <label style="display:flex;align-items:center;gap:6px;cursor:pointer;"><input type="radio" name="nt-mode" value="llm" checked>智能拆解(LLM)</label>
        <label style="display:flex;align-items:center;gap:6px;cursor:pointer;"><input type="radio" name="nt-mode" value="template">模板(开发→验证)</label>
      </div>
      <div style="display:flex;justify-content:flex-end;gap:9px;margin-top:18px;">
        <button onClick="{{ closeModal }}" style="height:34px;padding:0 14px;border-radius:9px;background:#fff;border:1px solid #E6E3DC;cursor:pointer;font-family:inherit;">取消</button>
        <button onClick="{{ submitTask }}" style="height:34px;padding:0 16px;border-radius:9px;background:{{ acc }};color:{{ accTxt }};border:1px solid {{ accBd }};font-weight:650;cursor:pointer;font-family:inherit;">下发</button>
      </div>
    </div>
  </div>
  </sc-if>

  <sc-if value="{{ modalAgent }}">
  <div style="position:fixed;inset:0;background:rgba(20,18,14,.4);display:flex;align-items:center;justify-content:center;z-index:50;">
    <div style="background:#fff;border-radius:16px;padding:22px;width:460px;box-shadow:0 20px 60px rgba(0,0,0,.25);">
      <div style="font-size:16px;font-weight:700;margin-bottom:14px;">新建 Agent</div>
      <input id="na-name" placeholder="名字,如 Gemini 开发" style="width:100%;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;margin-bottom:9px;">
      <input id="na-cmd" placeholder="命令,如 gemini" style="width:100%;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;margin-bottom:9px;">
      <input id="na-args" placeholder="参数(空格分隔),如 -p --yolo" style="width:100%;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;margin-bottom:9px;">
      <input id="na-model" placeholder="模型(可选)" style="width:100%;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;margin-bottom:9px;">
      <input id="na-caps" placeholder="能力(逗号分隔),如 代码生成,重构" style="width:100%;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;">
      <div style="display:flex;justify-content:flex-end;gap:9px;margin-top:18px;">
        <button onClick="{{ closeModal }}" style="height:34px;padding:0 14px;border-radius:9px;background:#fff;border:1px solid #E6E3DC;cursor:pointer;font-family:inherit;">取消</button>
        <button onClick="{{ submitAgent }}" style="height:34px;padding:0 16px;border-radius:9px;background:{{ acc }};color:{{ accTxt }};border:1px solid {{ accBd }};font-weight:650;cursor:pointer;font-family:inherit;">创建</button>
      </div>
    </div>
  </div>
  </sc-if>

  <sc-if value="{{ modalPerson }}">
  <div style="position:fixed;inset:0;background:rgba(20,18,14,.4);display:flex;align-items:center;justify-content:center;z-index:50;">
    <div style="background:#fff;border-radius:16px;padding:22px;width:460px;box-shadow:0 20px 60px rgba(0,0,0,.25);">
      <div style="font-size:16px;font-weight:700;margin-bottom:14px;">新建人员</div>
      <input id="np-name" placeholder="姓名" style="width:100%;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;margin-bottom:9px;">
      <input id="np-email" placeholder="邮箱(可选)" style="width:100%;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;margin-bottom:9px;">
      <input id="np-role" placeholder="角色,如 开发负责人" style="width:100%;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;margin-bottom:12px;">
      <div style="font-size:12px;color:#8A857C;margin-bottom:7px;">分配 Agent</div>
      <div style="display:flex;flex-wrap:wrap;gap:8px;">
        <sc-for list="{{ agents }}" as="a" hint-placeholder-count="2">
          <label style="display:flex;align-items:center;gap:6px;font-size:12.5px;border:1px solid #ECEAE4;border-radius:7px;padding:5px 9px;cursor:pointer;"><input type="checkbox" class="np-agent" value="{{ a.id }}">{{ a.name }}</label>
        </sc-for>
      </div>
      <div style="display:flex;justify-content:flex-end;gap:9px;margin-top:18px;">
        <button onClick="{{ closeModal }}" style="height:34px;padding:0 14px;border-radius:9px;background:#fff;border:1px solid #E6E3DC;cursor:pointer;font-family:inherit;">取消</button>
        <button onClick="{{ submitPerson }}" style="height:34px;padding:0 16px;border-radius:9px;background:{{ acc }};color:{{ accTxt }};border:1px solid {{ accBd }};font-weight:650;cursor:pointer;font-family:inherit;">创建</button>
      </div>
    </div>
  </div>
  </sc-if>
```

接线死按钮(改 onClick 绑定):
- L113 头部「新建任务」:`onClick="{{ newTask }}"` → 已会被 wiring 覆盖为打开弹窗(见 Step2 newTask 改为开弹窗)。保持不变。
- L336 画布「下发」:加 `onClick="{{ newTask }}"`。
- L357 「重新编排」:加 `onClick="{{ newTask }}"`。
- L395 「批准并运行」:加 `onClick="{{ newTask }}"`。
- Agent 名册标题区:在 `Agent 团队</h1>` 同级加一个「新建 Agent」按钮 `<button onClick="{{ newAgent }}" ...>+ 新建 Agent</button>`(放到该视图标题 div 内,右对齐)。
- 人员标题区:同样加「新建人员」按钮 `onClick="{{ newPerson }}"`;人员每行末尾加「分配」按钮 `onClick="{{ p.assign }}"`。

> 具体插入位置:Agent 名册视图 `<div style="margin-bottom:18px;">...Agent 团队...</div>` 改为 flex 两端对齐并加按钮;人员视图同理;人员行(`isPeople` 的 sc-for)在最后一个 `<span ... last ...>` 后加一个分配按钮单元格。完整片段在实现时按文件现状就近插入,保持原样式风格。

- [ ] **Step 2: 逻辑(改 wiring.js 的 Maestro 子类)**

在 `class Maestro` 内新增/调整方法:
```js
  // 弹窗开关:用 state.modal 标记
  newTask() { this.setState({ modal: 'task' }); }
  newAgent() { this.setState({ modal: 'agent' }); }
  newPerson() { this.setState({ modal: 'person', assignPid: null }); }
  closeModal() { this.setState({ modal: null }); }

  submitTask() {
    const text = (document.getElementById('nt-text') || {}).value;
    if (!text || !text.trim()) return;
    const proj = (document.getElementById('nt-proj') || {}).value || '';
    const modeEl = document.querySelector('input[name="nt-mode"]:checked');
    const mode = modeEl ? modeEl.value : 'llm';
    fetch('/task', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ text: text.trim(), project: proj, mode }) })
      .then((r) => r.json()).then(() => { this.setState({ modal: null, screen: 'tasks' }); setTimeout(() => this.fetchAll(), 300); }).catch(() => {});
  }
  submitAgent() {
    const g = (id) => (document.getElementById(id) || {}).value || '';
    const name = g('na-name'), command = g('na-cmd');
    if (!name.trim() || !command.trim()) return;
    const body = { name: name.trim(), command: command.trim(), args: g('na-args').split(/\s+/).filter(Boolean), model: g('na-model'), caps: g('na-caps').split(/[,，]/).map((s) => s.trim()).filter(Boolean) };
    fetch('/api/agents', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify(body) })
      .then((r) => r.json()).then(() => { this.setState({ modal: null }); this.fetchAll(); }).catch(() => {});
  }
  submitPerson() {
    const g = (id) => (document.getElementById(id) || {}).value || '';
    const name = g('np-name');
    if (!name.trim()) return;
    const ids = [...document.querySelectorAll('.np-agent:checked')].map((c) => c.value);
    fetch('/api/people', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ name: name.trim(), email: g('np-email'), role: g('np-role') }) })
      .then((r) => r.json()).then((d) => {
        if (ids.length && d.id) return fetch('/api/people/' + d.id + '/agents', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ agentIds: ids }) });
      }).then(() => { this.setState({ modal: null }); this.fetchAll(); }).catch(() => {});
  }
```

在 `renderVals()` 末尾(return v 前)加弹窗标志 + 每行分配 + 控制台:
```js
    v.modalTask = this.state.modal === 'task';
    v.modalAgent = this.state.modal === 'agent';
    v.modalPerson = this.state.modal === 'person';
    v.newTask = () => this.newTask();
    v.newAgent = () => this.newAgent();
    v.newPerson = () => this.newPerson();
    v.closeModal = () => this.closeModal();
    v.submitTask = () => this.submitTask();
    v.submitAgent = () => this.submitAgent();
    v.submitPerson = () => this.submitPerson();
    // 人员行分配按钮:为每个 people 注入 assign()
    (v.people || []).forEach((p) => { p.assign = () => this.assignPerson(p.id); });
    // 每 agent 实时控制台:agent 详情页日志框用累积缓冲
    v.agentLog = (this.state.console && this.state.console[this.state.agentId]) || (this.state.log[this.state.agentId] || []);
```

新增分配(简化:复用新建人员弹窗的勾选?改为直接弹窗勾选并保存到已有 pid):
```js
  assignPerson(pid) { this.setState({ modal: 'person', assignPid: pid }); }
```
> 简化:`submitPerson` 若 `this.state.assignPid` 存在,则跳过创建、直接对该 pid `POST .../agents`。在 submitPerson 开头加:
```js
    if (this.state.assignPid) {
      const ids = [...document.querySelectorAll('.np-agent:checked')].map((c) => c.value);
      return fetch('/api/people/' + this.state.assignPid + '/agents', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ agentIds: ids }) })
        .then(() => { this.setState({ modal: null, assignPid: null }); this.fetchAll(); }).catch(() => {});
    }
```
（弹窗标题在 assign 模式下仍叫"新建人员"亦可;若要更准,可加 `v.modalPersonTitle`,本期从简。）

每 agent 控制台:WS log 事件累积到 `state.console[agent]`:
在 `openWS` 的 onmessage 里,`m.type === 'log'` 分支加:
```js
        if (m.type === 'log') {
          if (m.agent) {
            const c = this.state.console || (this.state.console = {});
            (c[m.agent] = c[m.agent] || []).push('[T' + m.taskId + '·' + (m.stepId || '') + '] ' + m.data);
            c[m.agent] = c[m.agent].slice(-200);
            this.scheduleRender();
          }
        }
```
> 注意:原 openWS 的 `else if (m.type==='plan'||'status'||'task'||'log')` 里已含 'log' 触发 fetchAll;保留它（刷新真实数据）。把上面这段加在该分支之前或之内均可,关键是 log 事件既累积控制台又刷新。建议:把 'log' 从 fetchAll 触发里去掉(log 太频),只做控制台累积;plan/status/task 才 fetchAll。

进入 agent 详情时初始化控制台为历史日志:在 `go` 覆盖里:
```js
  go(screen, extra) {
    super.go(screen, extra);
    if (screen === 'task' && extra && typeof extra.taskId === 'number') this.fetchRelay(extra.taskId);
    if (screen === 'agent' && extra && extra.agentId) {
      fetch('/api/agentlog/' + extra.agentId).then((r) => r.json()).then((lines) => {
        const c = this.state.console || (this.state.console = {});
        c[extra.agentId] = lines || [];
        this.scheduleRender();
      }).catch(() => {});
    }
  }
```
（移除原 `fetchAgentLog` 调用,改由此处统一。）

- [ ] **Step 3: 重建 + 起服务 + 冒烟**

```bash
node <scratchpad>/build.js
# 重启 server(见 Task 6 联测),浏览器开 localhost:3000
```
手动核对:
- 画布「下发」「批准并运行」点开新建任务弹窗;填目标→下发→任务出现在列表并开始跑。
- Agent 名册「新建 Agent」→ 填 Gemini → 出现在名册。
- 人员「新建人员」→ 勾选 agent → 创建;某人「分配」→ 改勾选→保存。
- 进某 agent 详情,运行任务时日志框实时滚动且带 `[T..·..]` 标注。

- [ ] **Step 4: Commit**

```bash
git add orch/web/index.html orch/web/app.js
git commit -m "feat: 前端弹窗(新建任务/Agent/人员+分配)+接线下发+每agent实时控制台"
```

---

### Task 8: 端到端真跑 + README

**Files:**
- Modify: `orch/README.md`

- [ ] **Step 1: 全量回归**

Run: `cd orch && npm test`
预期:store/store2/echo/generic/engine/workspace/planner/planner2/runner 全 PASS。

- [ ] **Step 2: 真跑 LLM 深度拆解**

在一个 git 仓 `node /d/swap/orch/server.js`,浏览器新建任务「开发一个简单电商官网:首页+商品页+购物车,各一个 html」,模式选「智能拆解」。
预期:claude 产出多步 plan(可能 scaffold→各页面→验证),引擎并发跑就绪步骤,Agent 详情可见 claude 跨步骤并发日志;最终任务 done 且 git 仓里真生成多个 html 文件。

- [ ] **Step 3: README**

更新 `orch/README.md`:新增「新建 Agent / 新建人员与分配 / 智能拆解模式 / 每 agent 实时控制台」说明各一两句。

- [ ] **Step 4: Commit**

```bash
git add orch/README.md
git commit -m "docs: orch v2 使用说明"
```

---

## 自检结果

- **Spec 覆盖**:数据模型(T1)、可配置 agent 通用适配器(T2)、LLM 拆解+校验+回退(T3)、log 带 agent(T4)、api 读 DB(T5)、server 端点+注册表+mode(T6)、前端弹窗+接线+控制台(T7)、e2e+文档(T8)。#1=T6/T7,#2=T4/T7,#3=T1/T2/T5/T6/T7,#4=T1/T5/T6/T7。全覆盖。
- **占位符**:无 TBD;代码步骤均含完整代码。
- **类型一致**:`makePlan(text,{mode,agents,templatesDir,claude})`、`generic.make(def)`、`store.listAgents/addAgent/listPeople/addPerson/setPersonAgents/listPersonAgents/seed`、`roleMap(store)`、事件含 `agent` 字段 —— 前后一致。
- **风险**:api.js 重构(ROLE→roleMap)需把 buildAll/relay/plan/agentLog 四处都加 `const ROLE=roleMap(store)`;server toActivity 同步改。已在 T5/T6 标注。
