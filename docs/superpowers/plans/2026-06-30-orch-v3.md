# orch v3 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agent 改/删、项目新增+任务归属+全局搜索、当前用户过滤 Agent、总控台布局修复、产出物归档到 data/<用户>/<项目>/<任务>。

**Architecture:** 后端加 store 方法(agent update/delete、projects、owner)与 workspace 的 slug/taskDir;server 加端点并按任务建 data 目录;前端加弹窗/搜索视图/身份切换并就近修布局。引擎不变。

**Tech Stack:** Node 18+,express,ws,better-sqlite3,js-yaml,内置 `node --test`。前端原生 JS + dc 运行时。

## Global Constraints

- 依赖只允许:express、ws、better-sqlite3、js-yaml。不新增依赖。
- 平台 Windows:spawn `shell:true`;路径 `path.join`。
- 前端表单用非受控 input + `getElementById` 读值;morph 保留 DOM 节点不丢焦点。
- 身份过滤仅影响视图,不校验权限(无登录)。
- data 目录用 `slug` 做文件名安全;`.gitignore` 忽略 `data/`。

---

### Task 1: store — agent 改/删、projects、tasks.owner

**Files:**
- Modify: `orch/store.js`
- Test: `orch/test/store3.test.js`

**Interfaces:**
- Produces: `updateAgent(id,def)`、`deleteAgent(id)`(级联删 person_agents)、`addProject({name,client})=>id`、`listProjects()`、`createTask(text,project,owner)`（owner 落库,listTasks 返回 owner）。

- [ ] **Step 1: 写失败测试**

`orch/test/store3.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { open } = require('../store');

test('updateAgent 改字段', () => {
  const s = open(':memory:'); s.seed();
  s.updateAgent('claude', { name: 'Claude X', command: 'claude', args: ['-p'], model: 'm', caps: ['a'], color: '#111', avatar: 'C', dept: 'dev' });
  assert.equal(s.listAgents().find((a) => a.id === 'claude').name, 'Claude X');
});
test('deleteAgent 同时清分配', () => {
  const s = open(':memory:'); s.seed();
  const pid = s.addPerson({ name: '甲' }); s.setPersonAgents(pid, ['claude', 'codex']);
  s.deleteAgent('claude');
  assert.ok(!s.listAgents().some((a) => a.id === 'claude'));
  assert.deepEqual(s.listPersonAgents(pid), ['codex']);
});
test('项目新增与查', () => {
  const s = open(':memory:'); s.seed();
  const id = s.addProject({ name: '电商站', client: 'Acme' });
  assert.ok(s.listProjects().some((p) => p.id === id && p.name === '电商站'));
});
test('createTask 落 owner', () => {
  const s = open(':memory:'); s.seed();
  const id = s.createTask('做事', '项目A', '李四');
  assert.equal(s.listTasks().find((t) => t.id === id).owner, '李四');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/store3.test.js`
预期:FAIL（方法不存在 / owner undefined）。

- [ ] **Step 3: 实现**

`orch/store.js` 建表 `db.exec` 内追加 projects 表 + tasks 加 owner 列：
```js
    CREATE TABLE IF NOT EXISTS projects(
      id TEXT PRIMARY KEY, name TEXT, client TEXT, created_at TEXT);
```
把 tasks 表定义的列加 `owner TEXT`（改 `... project TEXT, owner TEXT, created_at TEXT, updated_at TEXT);`）。

`createTask` 改为带 owner：
```js
    createTask(text, project, owner) {
      const now = new Date().toISOString();
      return db.prepare('INSERT INTO tasks(text,status,project,owner,created_at,updated_at) VALUES(?,?,?,?,?,?)')
        .run(text, 'pending', project || '默认项目', owner || '操作者', now, now).lastInsertRowid;
    },
```
`listTasks` 的 SELECT 加 owner：`SELECT id,text,status,project,owner,created_at,updated_at FROM tasks ORDER BY id DESC`。

在返回对象里(放在 `seed()` 前)追加：
```js
    updateAgent(id, d) {
      db.prepare('UPDATE agents SET name=?,command=?,args=?,model=?,caps=?,color=?,avatar=?,dept=? WHERE id=?')
        .run(d.name || id, d.command || '', JSON.stringify(d.args || []), d.model || '—', JSON.stringify(d.caps || []), d.color || '#7C6FD9', d.avatar || (d.name || 'A').slice(0, 1).toUpperCase(), d.dept || 'dev', id);
    },
    deleteAgent(id) {
      db.prepare('DELETE FROM agents WHERE id=?').run(id);
      db.prepare('DELETE FROM person_agents WHERE agent_id=?').run(id);
    },
    addProject(d) {
      const base = (String(d.name || 'proj').toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '') || 'proj');
      const id = d.id || base + '-' + (db.prepare('SELECT COUNT(*) n FROM projects').get().n + 1);
      db.prepare('INSERT OR REPLACE INTO projects(id,name,client,created_at) VALUES(?,?,?,?)').run(id, d.name || id, d.client || '', new Date().toISOString());
      return id;
    },
    listProjects() { return db.prepare('SELECT * FROM projects').all(); },
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/store3.test.js`
预期:PASS,4 用例。

- [ ] **Step 5: 回归 + Commit**

Run: `cd orch && node --test`（全量,确保 owner 列变更未破坏 store/api 测试）
```bash
git add orch/store.js orch/test/store3.test.js
git commit -m "feat: store agent改删 + projects表 + tasks.owner"
```

---

### Task 2: workspace — slug + taskDir(每任务 data 目录)

**Files:**
- Modify: `orch/workspace.js`
- Test: `orch/test/workspace2.test.js`

**Interfaces:**
- Produces: `slug(s)`(文件名安全,保留中英数字/-/_,截断40,空→'x')、`taskDir(root,owner,project,text,id)`(建并返回 `root/data/<owner>/<project>/<text>-<id>`)。`makeWorkspace` 保留。

- [ ] **Step 1: 写失败测试**

`orch/test/workspace2.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const fs = require('fs');
const os = require('os');
const path = require('path');
const { slug, taskDir } = require('../workspace');

test('slug 文件名安全', () => {
  assert.equal(slug('开发 电商!@#站'), '开发-电商-站');
  assert.equal(slug(''), 'x');
});
test('taskDir 建出 data/owner/project/task 目录', () => {
  const root = fs.mkdtempSync(path.join(os.tmpdir(), 'orchroot-'));
  const dir = taskDir(root, '李四', '店铺', '做首页', 7);
  assert.ok(fs.existsSync(dir));
  assert.equal(path.basename(dir), '做首页-7');
  assert.ok(dir.includes(path.join('data', '李四', '店铺')));
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/workspace2.test.js`
预期:FAIL（slug/taskDir 未导出）。

- [ ] **Step 3: 实现**

`orch/workspace.js` 改为：
```js
const fs = require('fs');
const path = require('path');

// 文件名安全:保留各语言字母/数字/-/_,其余→-,首尾去-,截断
function slug(s) {
  const out = String(s == null ? '' : s).trim().replace(/[^\p{L}\p{N}\-_]+/gu, '-').replace(/^-+|-+$/g, '').slice(0, 40);
  return out || 'x';
}
// 每任务独立目录: root/data/<owner>/<project>/<text>-<id>
function taskDir(root, owner, project, text, id) {
  const dir = path.join(root, 'data', slug(owner), slug(project), slug(text) + '-' + id);
  fs.mkdirSync(dir, { recursive: true });
  return dir;
}
// 共享工作区(回退):同任务各步骤共用根目录
function makeWorkspace(rootRepo) {
  return { make() { return rootRepo; } };
}

module.exports = { makeWorkspace, slug, taskDir };
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/workspace2.test.js` 与 `node --test test/workspace.test.js`
预期:都 PASS（旧 workspace.test 仍测 makeWorkspace 共享）。

- [ ] **Step 5: Commit**

```bash
git add orch/workspace.js orch/test/workspace2.test.js
git commit -m "feat: workspace slug + 每任务 data 目录"
```

---

### Task 3: api — 项目合并(表 ∪ 任务派生)

**Files:**
- Modify: `orch/api.js`
- Test: `orch/test/api2.test.js`

**Interfaces:**
- Consumes: `store.listProjects()`。
- Produces: `buildAll(store).projects` 含表里项目(即使无任务)。

- [ ] **Step 1: 写失败测试**

`orch/test/api2.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { open } = require('../store');
const api = require('../api');

test('buildAll 合并表项目(无任务也出现)', () => {
  const s = open(':memory:'); s.seed();
  s.addProject({ name: '空项目X' });
  s.createTask('做事', '有任务项目', '操作者');
  const names = api.buildAll(s).projects.map((p) => p.name);
  assert.ok(names.includes('空项目X'));
  assert.ok(names.includes('有任务项目'));
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd orch && node --test test/api2.test.js`
预期:FAIL（空项目X 不在）。

- [ ] **Step 3: 实现**

`orch/api.js` 在 `const projects = Object.keys(projMap).map(...)` 之后、`const tasksVm = ...` 之前追加：
```js
  // 合并 projects 表里的项目(含无任务的空项目)
  store.listProjects().forEach((tp) => {
    if (!projMap[tp.name]) projects.push({ id: tp.id, name: tp.name, client: tp.client || 'orch', progress: 0, status: '规划', sk: 'queued', depts: [], agentCount: 0, taskCount: 0, tasks: [] });
  });
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd orch && node --test test/api2.test.js`
预期:PASS。

- [ ] **Step 5: Commit**

```bash
git add orch/api.js orch/test/api2.test.js
git commit -m "feat: api 项目合并 表+任务派生"
```

---

### Task 4: server — agent PUT/DELETE、项目端点、按 data 建任务目录

**Files:**
- Modify: `orch/server.js`
- Modify: `orch/.gitignore`

**Interfaces:**
- Produces: `PUT /api/agents/:id`、`DELETE /api/agents/:id`、`POST /api/projects`、`GET /api/projects`;`POST /task` 收 `user` 并把工作区设为 `data/<user>/<project>/<task>`。

- [ ] **Step 1: 实现**

`orch/server.js`:
引入 taskDir：把 `const { makeWorkspace } = require('./workspace');` 改为 `const { makeWorkspace, taskDir } = require('./workspace');`。

在 `app.post('/api/agents'...)` 附近追加：
```js
app.put('/api/agents/:id', (req, res) => { store.updateAgent(req.params.id, req.body || {}); adapters = buildAdapters(); broadcastRaw({ type: 'agents' }); res.json({ ok: true }); });
app.delete('/api/agents/:id', (req, res) => { store.deleteAgent(req.params.id); adapters = buildAdapters(); broadcastRaw({ type: 'agents' }); res.json({ ok: true }); });
app.get('/api/projects', (req, res) => res.json(store.listProjects()));
app.post('/api/projects', (req, res) => res.json({ id: store.addProject(req.body || {}) }));
```

把 `POST /task` 改为带 owner + 每任务目录：
```js
app.post('/task', (req, res) => {
  const owner = req.body.user || '操作者';
  const project = req.body.project || '默认项目';
  const id = store.createTask(req.body.text, project, owner);
  res.json({ id });
  let dir = ROOT;
  try { dir = taskDir(ROOT, owner, project, req.body.text, id); } catch (e) {}
  runTask(id, {
    store, adapters, workspace: { make: () => dir },
    makePlan: (text) => makePlan(text, { mode: req.body.mode, agents: store.listAgents().map((a) => a.id), templatesDir, claude: adapters.claude }),
    onEvent: broadcast,
  });
});
```
> `workspace`(makeWorkspace 实例)仍用于其它场景;此处按任务覆盖为 data 目录。

`orch/.gitignore` 追加一行：`data/`

- [ ] **Step 2: 联测**

```bash
cd <某git目录>
node /d/swap/orch/server.js & sleep 2
curl -s -X POST localhost:3000/api/projects -H "content-type: application/json" -d '{"name":"测试项目"}'
curl -s localhost:3000/api/all | grep -o 测试项目 && echo OK-proj
curl -s -X PUT localhost:3000/api/agents/codex -H "content-type: application/json" -d '{"name":"Codex2","command":"codex","args":["exec"],"dept":"qa"}'
curl -s localhost:3000/api/all | grep -o Codex2 && echo OK-put
curl -s -X DELETE localhost:3000/api/agents/codex; curl -s localhost:3000/api/all | grep -oc codex || echo OK-del
```
预期:OK-proj、OK-put、删后无 codex。完事 taskkill。

- [ ] **Step 3: Commit**

```bash
git add orch/server.js orch/.gitignore
git commit -m "feat: server agent改删端点 + 项目端点 + 任务产出归档 data/"
```

---

### Task 5: 前端 — Agent 编辑/删除

**Files:** 设计源 `<scratchpad>/Maestro.html`、`<scratchpad>/wiring.js` → `build.js` 重建。

- [ ] **Step 1: 模板 — Agent 详情按钮改编辑/删除 + 弹窗支持编辑态**

Maestro.html，Agent 详情头部把「暂停 / 指派任务」两按钮替换为：
```html
<div style="display:flex;gap:8px;"><button onClick="{{ editCurAgent }}" style="height:34px;padding:0 13px;border-radius:9px;background:#fff;border:1px solid #E6E3DC;font-weight:600;font-size:12.5px;cursor:pointer;font-family:inherit;" style-hover="background:#FAF9F6">编辑</button><button onClick="{{ delCurAgent }}" style="height:34px;padding:0 13px;border-radius:9px;background:#fff;border:1px solid #F1CBA6;color:#B4541E;font-weight:600;font-size:12.5px;cursor:pointer;font-family:inherit;">删除</button></div>
```

Agent 弹窗(modalAgent)：标题用 `{{ agentModalTitle }}`，五个 input 加 `value` 绑定，提交按钮文案 `{{ agentSubmitLabel }}`：
- `<input id="na-name" value="{{ naName }}" ...>`、`na-cmd value="{{ naCmd }}"`、`na-args value="{{ naArgs }}"`、`na-model value="{{ naModel }}"`、`na-caps value="{{ naCaps }}"`。
- 标题 `<div ...>{{ agentModalTitle }}</div>`，提交 `<button onClick="{{ submitAgent }}" ...>{{ agentSubmitLabel }}</button>`。

- [ ] **Step 2: 逻辑(wiring.js)**

`class Maestro` 内：
```js
  editCurAgent() {
    const a = this.AGENTS.find((x) => x.id === this.state.agentId);
    if (a) this.setState({ modal: 'agent', editAgent: a });
  }
  delCurAgent() {
    const id = this.state.agentId;
    if (!id || !window.confirm('删除该 Agent?')) return;
    fetch('/api/agents/' + id, { method: 'DELETE' }).then(() => { this.setState({ screen: 'agents' }); this.fetchAll(); }).catch(() => {});
  }
```
`newAgent()` 改为清编辑态：`newAgent() { this.setState({ modal: 'agent', editAgent: null }); }`

`submitAgent()` 改为 PUT/POST 二选一：
```js
  submitAgent() {
    const g = (id) => (document.getElementById(id) || {}).value || '';
    const name = g('na-name'), command = g('na-cmd');
    if (!name.trim() || !command.trim()) return;
    const body = { name: name.trim(), command: command.trim(), args: g('na-args').split(/\s+/).filter(Boolean), model: g('na-model'), caps: g('na-caps').split(/[,，]/).map((s) => s.trim()).filter(Boolean) };
    const ea = this.state.editAgent;
    const url = ea ? '/api/agents/' + ea.id : '/api/agents';
    const method = ea ? 'PUT' : 'POST';
    if (ea) { body.color = ea.color; body.avatar = ea.avatar; body.dept = ea.dept; }
    fetch(url, { method, headers: { 'content-type': 'application/json' }, body: JSON.stringify(body) })
      .then((r) => r.json()).then(() => { this.setState({ modal: null, editAgent: null }); this.fetchAll(); }).catch(() => {});
  }
```

`renderVals()` 追加(在 modal 标志附近)：
```js
    const ea = this.state.editAgent;
    v.editCurAgent = () => this.editCurAgent();
    v.delCurAgent = () => this.delCurAgent();
    v.agentModalTitle = ea ? '编辑 Agent' : '新建 Agent';
    v.agentSubmitLabel = ea ? '保存' : '创建';
    v.naName = ea ? ea.name : '';
    v.naCmd = ea ? (ea.command || '') : '';
    v.naArgs = ea ? (Array.isArray(ea.args) ? ea.args.join(' ') : '') : '';
    v.naModel = ea && ea.model && ea.model !== '—' ? ea.model : '';
    v.naCaps = ea && ea.caps ? ea.caps.join(',') : '';
```
> 注:api 的 agent 对象 `args` 未返回(buildAll 不含 args);编辑预填 args 需要原始 args。解决:让 api buildAll 的 agent 增加 `args: <原始数组>`。在 api.js agent 映射里加 `args: (()=>{try{return JSON.parse(<rawAgentRow>.caps?)...}})()` —— 更简单:roleMap 里已 parse caps;另存 args。**在 Task 5 一并改 api**:roleMap 增 `args`,buildAll agent 增 `args: r.args`。

补 api.js：`roleMap` 内 `m[a.id]` 增 `args: (()=>{try{return JSON.parse(a.args);}catch(e){return [];}})()`;buildAll agent 对象增 `args: r.args`。

- [ ] **Step 3: 重建 + 冒烟**

`node <scratchpad>/build.js`;重启 server;浏览器进某 agent 详情 → 编辑(改名保存,名册更新) / 删除(确认后消失)。

- [ ] **Step 4: Commit**

```bash
git add orch/web/index.html orch/web/app.js orch/api.js
git commit -m "feat: 前端 Agent 编辑/删除 + api 返回 args"
```

---

### Task 6: 前端 — 项目新增 + 任务选项目 + 全局搜索

**Files:** `<scratchpad>/Maestro.html`、`<scratchpad>/wiring.js`、`orch/web/runtime.js`(加 onkeydown 支持)。

- [ ] **Step 1: runtime 支持 onkeydown(给搜索框回车用)**

`orch/web/runtime.js` 的属性处理里,`onclick` 分支后加 onkeydown：
```js
      } else if (name === 'onkeydown') {
        el._onkeydown = resolve(strip(val), scopes);
        el.addEventListener('keydown', dispatchKey);
```
顶部 dispatchClick 旁加：`function dispatchKey(e){ const f=e.currentTarget._onkeydown; if(typeof f==='function') f(e); }`
morphNode 同步：`o._onkeydown = nw._onkeydown;`

- [ ] **Step 2: 模板 — 项目新建弹窗 + 新建任务项目下拉 + 搜索 input + 搜索视图**

(a) 顶栏搜索框(原 div)改为真 input + 触发：
把搜索那块 `<div ...搜索 Agent...>` 内的占位 span 换成：
```html
<input id="search" onKeyDown="{{ onSearchKey }}" placeholder="搜索 Agent、任务、项目…" style="flex:1;border:none;outline:none;background:transparent;font-size:13px;font-family:inherit;color:#1A1814;">
```
(保留外层带放大镜 svg 的容器与 ⌘K 角标)

(b) 项目页标题区(`项目</h1>` 那行)改 flex 两端 + 加按钮：
```html
<button onClick="{{ newProject }}" style="height:34px;padding:0 14px;border-radius:9px;background:{{ acc }};color:{{ accTxt }};border:1px solid {{ accBd }};font-weight:650;font-size:13px;cursor:pointer;font-family:inherit;">+ 新建项目</button>
```

(c) 新建任务弹窗:把 `<input id="nt-proj" ...>` 替换为下拉 + 新建框：
```html
<select id="nt-proj-sel" style="width:100%;margin-top:10px;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;font-size:13px;box-sizing:border-box;">
  <option value="">默认项目</option>
  <sc-for list="{{ projOpts }}" as="p" hint-placeholder-count="3"><option value="{{ p.name }}">{{ p.name }}</option></sc-for>
</select>
<input id="nt-proj-new" placeholder="或填新项目名(优先)" style="width:100%;margin-top:8px;height:32px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;font-size:12.5px;box-sizing:border-box;">
```

(d) 项目弹窗(新)+ 搜索视图(新),在 `</x-dc>` 前加：
```html
  <sc-if value="{{ modalProject }}">
  <div style="position:fixed;inset:0;background:rgba(20,18,14,.4);display:flex;align-items:center;justify-content:center;z-index:50;">
    <div style="background:#fff;border-radius:16px;padding:22px;width:420px;box-shadow:0 20px 60px rgba(0,0,0,.25);">
      <div style="font-size:16px;font-weight:700;margin-bottom:14px;">新建项目</div>
      <input id="pr-name" placeholder="项目名称" style="width:100%;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;margin-bottom:9px;box-sizing:border-box;">
      <input id="pr-client" placeholder="客户(可选)" style="width:100%;height:34px;border:1px solid #E6E3DC;border-radius:9px;padding:0 10px;font-family:inherit;box-sizing:border-box;">
      <div style="display:flex;justify-content:flex-end;gap:9px;margin-top:18px;">
        <button onClick="{{ closeModal }}" style="height:34px;padding:0 14px;border-radius:9px;background:#fff;border:1px solid #E6E3DC;cursor:pointer;font-family:inherit;">取消</button>
        <button onClick="{{ submitProject }}" style="height:34px;padding:0 16px;border-radius:9px;background:{{ acc }};color:{{ accTxt }};border:1px solid {{ accBd }};font-weight:650;cursor:pointer;font-family:inherit;">创建</button>
      </div>
    </div>
  </div>
  </sc-if>
```

搜索视图:在 main 内最后一个 sc-if(people)之后加：
```html
      <sc-if value="{{ isSearch }}">
      <div style="padding:26px 30px 40px;max-width:1080px;">
        <div style="margin-bottom:18px;"><h1 style="font-size:21px;font-weight:700;margin:0;">搜索 "{{ q }}"</h1><div style="font-size:13px;color:#8A857C;margin-top:4px;">{{ searchSummary }}</div></div>
        <sc-for list="{{ searchGroups }}" as="g" hint-placeholder-count="4">
          <div style="margin-bottom:18px;">
            <div style="font-size:12px;font-weight:600;letter-spacing:.05em;text-transform:uppercase;color:#A39E94;margin-bottom:8px;">{{ g.label }}</div>
            <div style="background:#fff;border:1px solid #E9E7E1;border-radius:12px;overflow:hidden;">
              <sc-for list="{{ g.items }}" as="it" hint-placeholder-count="3">
                <button onClick="{{ it.open }}" style-hover="background:#FAF9F6" style="width:100%;text-align:left;border:none;border-bottom:1px solid #F2F0EB;background:transparent;cursor:pointer;display:flex;align-items:center;gap:10px;padding:11px 16px;font-family:inherit;">
                  <span style="font-size:13.5px;font-weight:600;">{{ it.title }}</span><span style="font-size:12px;color:#A39E94;">{{ it.sub }}</span>
                </button>
              </sc-for>
            </div>
          </div>
        </sc-for>
      </div>
      </sc-if>
```

- [ ] **Step 3: 逻辑(wiring.js)**

```js
  newProject() { this.setState({ modal: 'project' }); }
  submitProject() {
    const name = (document.getElementById('pr-name') || {}).value || '';
    if (!name.trim()) return;
    const client = (document.getElementById('pr-client') || {}).value || '';
    fetch('/api/projects', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ name: name.trim(), client }) })
      .then((r) => r.json()).then(() => { this.setState({ modal: null }); this.fetchAll(); }).catch(() => {});
  }
  onSearchKey(e) { if (e.key === 'Enter') { const q = (e.currentTarget.value || '').trim(); if (q) this.go('search', { q }); } }
  searchResults(q) {
    const lc = q.toLowerCase();
    const hit = (s) => String(s || '').toLowerCase().includes(lc);
    const groups = [];
    const tasks = this.TASKS.filter((t) => hit(t.title) || hit(t.proj)).map((t) => ({ title: t.title, sub: t.proj, open: () => this.go('task', { taskId: t.id }) }));
    const agents = this.AGENTS.filter((a) => hit(a.name) || hit(a.id)).map((a) => ({ title: a.name, sub: a.model || '', open: () => this.go('agent', { agentId: a.id }) }));
    const projects = this.PROJECTS.filter((p) => hit(p.name) || hit(p.client)).map((p) => ({ title: p.name, sub: p.client || '', open: () => this.go('project', { projectId: p.id }) }));
    const people = this.PEOPLE.filter((p) => hit(p.name) || hit(p.role)).map((p) => ({ title: p.name, sub: p.role || '', open: () => this.go('people') }));
    if (tasks.length) groups.push({ label: '任务', items: tasks });
    if (agents.length) groups.push({ label: 'Agent', items: agents });
    if (projects.length) groups.push({ label: '项目', items: projects });
    if (people.length) groups.push({ label: '人员', items: people });
    return groups;
  }
```

`renderVals()` 追加：
```js
    v.isSearch = this.state.screen === 'search';
    v.q = this.state.q || '';
    v.modalProject = this.state.modal === 'project';
    v.newProject = () => this.newProject();
    v.submitProject = () => this.submitProject();
    v.onSearchKey = (e) => this.onSearchKey(e);
    v.projOpts = (this.PROJECTS || []).map((p) => ({ name: p.name }));
    if (v.isSearch) { v.searchGroups = this.searchResults(this.state.q || ''); v.searchSummary = v.searchGroups.reduce((a, g) => a + g.items.length, 0) + ' 条结果'; } else { v.searchGroups = []; v.searchSummary = ''; }
```

`submitTask()` 改读项目下拉/新项目 + 传 user：
```js
  submitTask() {
    const text = (document.getElementById('nt-text') || {}).value || '';
    if (!text.trim()) return;
    const sel = (document.getElementById('nt-proj-sel') || {}).value || '';
    const nw = (document.getElementById('nt-proj-new') || {}).value || '';
    const project = (nw.trim() || sel || '默认项目');
    const modeEl = document.querySelector('input[name="nt-mode"]:checked');
    const mode = modeEl ? modeEl.value : 'llm';
    const user = this.currentName();
    fetch('/task', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ text: text.trim(), project, mode, user }) })
      .then((r) => r.json()).then(() => { this.setState({ modal: null, screen: 'tasks' }); setTimeout(() => this.fetchAll(), 300); }).catch(() => {});
  }
  currentName() { const c = (this.PEOPLE || []).find((p) => p.id === this.state.currentPersonId); return c ? c.name : ((this.PEOPLE[0] && this.PEOPLE[0].name) || '操作者'); }
```
> `crumb` 搜索页:基类 labels 无 'search',`crumbLeaf` 为空可接受;可选在 renderVals 设 `if(v.isSearch){v.crumbRoot='工作区';v.crumbLeaf='搜索';}`。

- [ ] **Step 4: 重建 + 冒烟**

`node build.js`;重启;新建项目→项目页出现;新建任务下拉选到它;顶栏搜索输入回车→搜索结果页列出匹配。

- [ ] **Step 5: Commit**

```bash
git add orch/web/index.html orch/web/app.js orch/web/runtime.js
git commit -m "feat: 前端 项目新增 + 任务选项目 + 全局搜索 + runtime onkeydown"
```

---

### Task 7: 前端 — 当前用户切换过滤 Agent + 总控台布局修复

**Files:** `<scratchpad>/Maestro.html`、`<scratchpad>/wiring.js`。

- [ ] **Step 1: 模板 — 左下角身份可点 + who 弹窗 + 总控台 grid 修复**

(a) 侧栏底部用户块外层 div 加 `onClick="{{ pickWho }}"` 与 `cursor:pointer`，头像/名字用 `{{ opAv }}/{{ opName }}/{{ opRole }}`(已是绑定)。

(b) who 选择弹窗(在 `</x-dc>` 前加)：
```html
  <sc-if value="{{ modalWho }}">
  <div style="position:fixed;inset:0;background:rgba(20,18,14,.4);display:flex;align-items:center;justify-content:center;z-index:50;">
    <div style="background:#fff;border-radius:16px;padding:18px;width:340px;box-shadow:0 20px 60px rgba(0,0,0,.25);">
      <div style="font-size:15px;font-weight:700;margin-bottom:12px;">切换当前身份</div>
      <sc-for list="{{ whoList }}" as="w" hint-placeholder-count="3">
        <button onClick="{{ w.pick }}" style-hover="background:#FAF9F6" style="width:100%;display:flex;align-items:center;gap:10px;padding:9px 10px;border:none;border-radius:9px;background:transparent;cursor:pointer;font-family:inherit;text-align:left;">
          <span style="width:26px;height:26px;border-radius:50%;background:{{ w.color }};color:#fff;font-size:11px;font-weight:700;display:flex;align-items:center;justify-content:center;">{{ w.av }}</span>
          <span style="flex:1;"><span style="font-size:13px;font-weight:600;">{{ w.name }}</span> <span style="font-size:11.5px;color:#A39E94;">{{ w.role }}</span></span>
          <span style="font-size:11px;color:#A39E94;font-family:'JetBrains Mono',monospace;">{{ w.agents }} agent</span>
        </button>
      </sc-for>
    </div>
  </div>
  </sc-if>
```

(c) 总控台双栏 grid 修复:那个 `display:grid;grid-template-columns:1.55fr 1fr;` 的容器,两个直接子卡片 div 各加 `min-width:0;`;运行中 Agent 卡片内 `{{ a.action }}` 所在容器(及其 flex 父)就近加 `min-width:0;`。同样给活动卡片 div 加 `min-width:0;`。

- [ ] **Step 2: 逻辑(wiring.js)**

```js
  pickWho() { this.setState({ modal: 'who' }); }
```
`renderVals()` 追加(放在设置 opName 之后,覆盖为当前身份)：
```js
    const cur = (this.PEOPLE || []).find((p) => p.id === this.state.currentPersonId);
    if (cur) { v.opName = cur.name; v.opAv = cur.av; v.opRole = (cur.role || '成员') + ' · 当前身份'; }
    v.pickWho = () => this.pickWho();
    v.modalWho = this.state.modal === 'who';
    v.whoList = (this.PEOPLE || []).map((p) => ({ ...p, pick: () => this.setState({ modal: null, currentPersonId: p.id }) }));
    // 身份过滤:当前人有分配则 Agent 团队/运行中只显示其 agent
    if (cur && cur.assignedIds && cur.assignedIds.length) {
      const set = new Set(cur.assignedIds);
      v.agents = (v.agents || []).filter((a) => set.has(a.id));
      v.activeAgents = (v.activeAgents || []).filter((a) => set.has(a.id));
    }
```

- [ ] **Step 3: 重建 + 冒烟**

`node build.js`;重启;点左下角→选"李航"(被分配 2 agent)→Agent 团队只剩那 2 个;切回操作者→全部。总控台活动面板横向正常(不再竖排)。

- [ ] **Step 4: Commit**

```bash
git add orch/web/index.html orch/web/app.js
git commit -m "feat: 前端 当前身份切换过滤Agent + 总控台布局修复"
```

---

### Task 8: 端到端 + README

- [ ] **Step 1: 全量回归**

Run: `cd orch && npm test`
预期:全 PASS(store/store2/store3/workspace/workspace2/api/api2/echo/generic/engine/planner/planner2/runner)。

- [ ] **Step 2: 真跑产出归档**

git 目录 `node /d/swap/orch/server.js`;新建任务"创建 index.html 首页",项目填"店铺",当前身份"李航",智能拆解下发。
预期:运行后出现 `data/李航/店铺/创建-index-html-首页-<id>/` 目录,内有 agent 真实产出文件。

- [ ] **Step 3: README**

`orch/README.md` 增:Agent 可改/删、项目新建+任务归属、全局搜索、左下角切身份过滤 Agent、产出物归档 `data/<用户>/<项目>/<任务>` 各一两句。

- [ ] **Step 4: Commit**

```bash
git add orch/README.md
git commit -m "docs: orch v3 使用说明"
```

---

## 自检结果

- **Spec 覆盖**:#1=T1/T5,#2=T1/T3/T4/T6,#3=T7,#4=T7,#5=T1/T2/T4。全覆盖。
- **占位符**:无 TBD;代码步骤含完整代码。Task5 标注了"api 需返回 args"并给出改法(roleMap+buildAll 加 args),已闭环。
- **类型一致**:`updateAgent/deleteAgent/addProject/listProjects`、`slug/taskDir`、`createTask(text,project,owner)`、api agent 增 `args`、前端 `editAgent/currentPersonId/q` 状态前后一致。
- **风险**:morph 下表单 input 用 value 属性预填(编辑态)——morph 同步 value 属性会覆盖用户输入吗?value 属性仅设初值,用户输入改的是 property;morph 比较 value **属性**(模板渲染的初值)不变→不覆盖 property。可接受;若发现编辑态二次渲染回填,改用打开时一次性 setState 不再随渲染变。已在 T5 备注。
